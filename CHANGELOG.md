# Changelog

All notable changes to this project are documented here.

## Versioning scheme — CalVer (`YYYY.0M.MICRO`)

This project uses **CalVer** (calendar versioning), not SemVer. Each tag encodes the release month, with a per-month patch counter:

| Component | Meaning |
|---|---|
| `YYYY` | Four-digit year |
| `0M` | Zero-padded month (01–12) |
| `MICRO` | In-month patch counter (0 = monthly cycle release; 1+ = factual corrections, URL fixes, regulatory-watch entries that landed mid-month) |

Examples: `2026.04.0`, `2026.04.1`, `2026.05.0`, `2027.01.0`.

### What triggers a new tag

- **`.0` (monthly cycle)** — at end-of-month, an automated workflow tags `YYYY.0M.0` if any change landed on `main` since the previous tag. No-ops silently when nothing changed.
- **`.1`, `.2`, …** — manual, on factual corrections / URL replacement batches / regulatory-watch entries that supersede a Tier-1 ENDED programme.
- **What does NOT trigger a tag** — typo fixes, formatting, internal refactors, README polish without skill content change.

### Format of each entry

This file follows [Keep a Changelog](https://keepachangelog.com/) adapted for CalVer:

```
## [YYYY.0M.MICRO] - YYYY-MM-DD

### Added
- ...

### Changed
- ...

### Fixed
- ...

### Removed / Deprecated
- ...

### Breaking
- ... (use sparingly — schema or flag-rename changes)

### Security
- ...
```

When a release ends a programme (golden visa scrapped, NHR-style regime closed), it's documented under both `### Removed / Deprecated` AND surfaced in `shared/regulatory-watch.md` ENDED registry.

---

## [Unreleased]

### Added

- **`External PR triage` (`pr-triage.yml`) — a pre-approval risk summary for community PRs.** The fork-approval policy holds every `pull_request` check until a maintainer clicks "Approve and run", leaving nothing to decide on. This runs on `pull_request_target` (trusted base-branch context, so it fires regardless of approval settings) and posts a sticky comment grading the PR 🔴 touches `.github/`/`scripts/` · 🟠 non-Markdown · 🟢 Markdown-only. Never checks out PR code. ([#336](https://github.com/soreavis/property-deep-dive/pull/336))

### Changed

- **`CHANGELOG entry under [Unreleased]` now re-evaluates when a label changes.** The gate skips on any of `skip changelog` / `documentation` / `ci` / `dependencies` / `github-actions` / `duplicate` / `wontfix`, but only ran on `opened` / `synchronize` / `reopened` — so applying a skip label to an already-open PR left a stale red that needed a manual re-run. Added `labeled` / `unlabeled` to the trigger types. Note this fires for human- and PAT-applied labels only: GitHub's recursion prevention means a label applied by the path-based labeler's `GITHUB_TOKEN` emits no event.

- **Dropped the inert `schedule.day: monday` from `.github/dependabot.yml`.** `day` applies only to `weekly` intervals, so under `monthly` it was silently ignored and advertised a cadence that never existed (`monthly` runs on the 1st). `time`/`timezone` kept — those apply to every interval. Also annotated `open-pull-requests-limit: 5` as GitHub's own default. No behaviour change. ([#340](https://github.com/soreavis/property-deep-dive/pull/340))

- **`scripts/lock-requirements.py` — regenerating the pip lock no longer erases its own instructions.** `requirements.txt` opens with an 18-line header explaining the file and carrying the regeneration command, but running that command with `-o requirements.txt` overwrites the block — so the file lost its own instructions the first time anyone followed them. The helper preserves the header, pins resolution to the CI runner (3.12 / linux x86_64), and adds `--check` to catch a `requirements.in` edit that was never compiled in. Documented in `CONTRIBUTING.md`.

- **Releases `2026.04.0` → `2026.05.59` archived to `docs/changelog/2026-h1.md`.** Root `CHANGELOG.md` drops 512 KB → 174 KB and 946 → 359 lines, keeping `[Unreleased]` plus the two current releases; the archive is linked from the file's foot and the docs index. Archived text is frozen as published, so `check-prose-counts.py` now skips `docs/changelog/**` — "all 109 supported countries" was true at the time and must not be rewritten to today's count.

- **CHANGELOG bullets under `[Unreleased]` are capped at 600 characters.** The file reached 512 KB across only 946 lines and 14 releases — the driver was entry length, not release count (median bullet 288 chars, p90 2,467, longest 13,073). New `scripts/check-changelog-length.py` + a `CHANGELOG entries stay short` job enforce it; released sections are grandfathered. Detail belongs in the PR body, which is permanent and linked from the squash commit.

- **README generated line counts round to the nearest 1,000 above 10k, not 100.** A flat nearest-100 step meant a **six-line** PR could flip the six-figure repo total (109,254 → "109,300") and fail `Doc sync check` for a contributor who had touched nothing related — it happened twice in one session. `_round_100()` in `scripts/sync-docs.py` is now magnitude-aware (nearest 1,000 at 10k+, nearest 100 below), so the figure only moves on a change of real size. Sub-10k counts are unaffected.

- **`CONTRIBUTING.md` documents the two gates that catch first-time contributors** — the `CHANGELOG.md` requirement (with the skip-label list) and re-running `scripts/sync-docs.py` for generated doc counts — plus a fork-contributor note explaining that "waiting for approval" is the expected initial state, not a failure.

- fix(ci): stop shallow fetch grafting the base branch; tolerate auto-merge refusal ([#333](https://github.com/soreavis/property-deep-dive/pull/333)) — by @soreavis

### Fixed

- **A cancelled `Changelog enforcer` run left required-check PRs permanently BLOCKED.** Fallout of the `labeled` trigger above: opening a PR that already carries a label fires `opened` and `labeled` on the *same* SHA, and `cancel-in-progress` had the second cancel the first. A `cancelled` check-run for a **required** context blocks the merge even when a newer successful run exists — #337 showed 8/8 green and still refused to merge. Set `cancel-in-progress: false` here. ([#338](https://github.com/soreavis/property-deep-dive/pull/338))

- **`PR validate` intermittently died with `fatal: no merge base` — a shallow fetch was grafting the base branch.** Four jobs checked out at `fetch-depth: 0` then ran `git fetch --depth=1 origin <base>`, which re-grafts `origin/<base>` as a shallow ref and destroys the merge base `"$base...HEAD"` needs — so they aborted with exit 128 whenever `main` advanced past the PR's fork point. Three of the four are **required** checks, so this intermittently blocked merges. Dropped `--depth=1` at all four sites. ([#333](https://github.com/soreavis/property-deep-dive/pull/333))

- **`Dependabot auto-merge` went red on a GitHub restriction it can never satisfy.** Every `github_actions` bump edits `.github/workflows/*`, and GitHub intermittently refuses `enablePullRequestAutoMerge` for those — a scope `GITHUB_TOKEN` cannot hold, since no `workflows` key exists in the Actions `permissions` block. The job now retries 3× and downgrades a persistent refusal to a `::warning::`; any other error still fails loudly. Optional `AUTOMERGE_TOKEN` secret (Dependabot store, `Workflows: write`) makes it deterministic. ([#333](https://github.com/soreavis/property-deep-dive/pull/333))

- **`auto-tag.yml` could ship a release whose manifest was from another month.** It derives the tag purely from the calendar and never read `.claude-plugin/plugin.json`, while `Version month guard` only fires on PRs that *change* the version — so a month could roll with no bump and `sign-release.yml` would build the tarball from that tag with a stale manifest inside. Added a pre-tag guard: a CalVer **month** mismatch fails the run and prints the bump command; a same-month MICRO difference only warns, since mid-month MICRO releases legitimately run ahead of the monthly `.0`.

### Security

- **pypdf bumped `5.9.0` → `6.14.2`, clearing 35 advisories (2 high, 30 moderate, 3 low).** Every fix lands in the 6.x line, so the old `pypdf>=4,<6` constraint could never reach a patched version — CI had been installing a vulnerable pypdf since the cap was written. The exposure was pre-existing and invisible; putting the deps in a manifest ([#341](https://github.com/soreavis/property-deep-dive/pull/341)) is what surfaced it. `source-verify` uses only `PdfReader`/`.pages`/`.extract_text()`, unchanged across the major.

- **CI Python dependencies are hash-pinned and Dependabot-tracked.** `source-verify.yml` and `url-liveness.yml` installed `aiohttp`/`pypdf` from inline version ranges — the install trusted whatever the index served, and Dependabot could not see them at all (it tracks declared manifests, never an inline `pip install`). Added a hash-pinned root `requirements.txt` with `requirements.in` as source; both workflows install with `--require-hashes`, and a `pip` ecosystem now tracks them. ([#341](https://github.com/soreavis/property-deep-dive/pull/341))

## [2026.07.0] - 2026-07-31

### Added

- **EU off-plan mortgages: Commission Delegated Regulation (EU) 2026/849 recorded and wired into `--type=off-plan`.** Surfaced by the feed watcher (#311–#313) and triaged against primary sources. The RTS sets what counts as an *equivalent legal mechanism* — a completion guarantee — under CRR art. 124(3)(a)(iii)(2), i.e. when an EU lender may keep the preferential immovable-property risk weight on a home still under construction. Adopted 16 Apr 2026 (C/2026/2429), published **OJ L, 2026/849, 23.7.2026**, in force the 20th day after publication → **applies 12 Aug 2026** (all four facts read off the EUR-Lex ELI record, fetched directly, HTTP 200); built on EBA final draft `EBA/RTS/2025/05` (submitted 5 Aug 2025) and EBA opinion `EBA/Op/2026/03` (20 Feb 2026). Added as an EU-wide row in `shared/regulatory-watch.md` plus a rule that it is a **bank-side prudential rule, never a buyer obligation**, and that whether a national scheme (FR *garantie financière d'achèvement*, ES *aval bancario*, IT *polizza fideiussoria*, …) satisfies all of art. 1(2)–(13) is the lender's assessment and must not be asserted. `shared/property-types.md` § Off-plan gains the buyer-facing consequence next to its existing guarantees-by-country table: the art. 1 conditions that bear on the mortgage offer (guarantee required by the law of the build's member state, provider a credit institution or insurer outside the lender's group, timely unconditional activation, one guarantee covering every unit in the building) and the question to put to the lender early. No country playbook figures changed.

- **FR DROM overlay wired into the router — overseas départements now load their deltas.** The 254-line `shared/fr-drom-overlay.md` (Guadeloupe / Martinique / Guyane / Réunion / Mayotte — DROM TVA rates, *octroi de mer*, *taxe foncière* abattements, majored notary *émoluments*, Letchimy indivision regime, cyclone/seismic hazard profiles) existed but was referenced **nowhere in the skill payload**, so a DROM address (postcode `971xx`–`976xx`, detected as `fr`) loaded metropolitan FR rules with none of the overseas deltas. Added a DROM-detection annex to `countries/fr/playbook.md` and a routing note to `SKILL.md`'s load step, so the overlay is now applied on top of the FR baseline for `gp`/`mq`/`gf`/`re`/`yt`. Wiring only — no figures invented.

- **Anti-hallucination Check 7 (foreign-language preservation) written into the validator.** The contract heading read "the eight mandatory pre-output checks" but the body stopped at Check 7 (Contradiction surfacing) — PR #180 renumbered the count to eight but never wrote the new check in, leaving the MANDATORY gate one check short and the docs split 7-vs-8 across five files. Added `### Check 7: Foreign-language preservation` (every native statute / regulator / form / registry / programme / tax name must appear in original form alongside the English gloss; grounded in `shared/foreign-language-preservation.md`), renumbered Contradiction surfacing to Check 8, and reconciled the straggler "7/seven" references across `SKILL.md`, `README.md`, `CLAUDE.md`, and `updater.md` to eight.

- **CalVer drift guard — `Version month guard` required check + `scripts/next-version.py`.** Closes the one gap that let `plugin.json` drift to `2026.05.71` deep into June: the version counter could be bumped into a stale month's namespace with nothing to catch it. New `scripts/next-version.py` is the single source of truth for the rule (`0M` = current calendar month; `MICRO` resets to `0` on a month roll; monotonic within a month) — it computes the correct next version (`--write` bumps `plugin.json` in place) and validates a proposed one (`--check HEAD --base BASE`). A new **`Version month guard`** job in `pr-validate.yml` calls `--check` on every PR (diff-gated: instant pass when the version is unchanged) and is a **required** branch-protection check, so any bump landing in the wrong month / skipping the MICRO reset / decreasing is blocked at merge with the correct number in the failure message. Release SOP in `CONTRIBUTING.md` § Release process + the repo `CLAUDE.md` now point at the helper. Tooling/governance only — no skill-content change.

### Changed

- **Gibraltar Frontier Treaty — in-force patch (pre-staged for 15 Jul 2026, merged on/after provisional application began).** `countries/gi/playbook.md` §§ foreign-buyer / `--frontier-treaty` / `--integrity` / quirks flipped from "once provisionally applied" to the in-force state: land-frontier immigration checks + barriers removed, dual Schengen checks at Gibraltar airport/port (Gibraltar immigration + Spanish officials for Schengen-area integrity — FCDO summary), residents exempt EES/ETIAS/wet-stamping regardless of Civilian Registration Card colour (HMGoG PR 148/2026 + 234/2026), customs union + 3-year €300 travellers'-allowance transition + goods-in-transit rule (HMGoG treaty portal), category-4 no-climb fencing + facial-recognition/ANPR frontier security (PR 517/2026, 2 Jul 2026), ratification-pending status (EP consent expected autumn 2026 + UK CRAG — Commons Library CBP-10572), and the FCDO §14 nuance that residence-permit approval gains an EU-facing coordination step while competence stays with Gibraltar. NEW: the domestic **Residence Criteria framework** (HMGoG PR 466/2026, 17 Jun 2026 — genuine-economic-contribution basis, regulations pending) documented in the playbook, and the regulatory-watch Cat-2 entry upgraded tier 4 → 2 (the r/gibraltar community signal is now primary-confirmed in direction; Cat-2-specific changes still unconfirmed). Honest gaps kept explicit: EES handling for non-resident British at the airport unconfirmed in published guidance; treaty-gov.info flagged as an official HMGoG microsite pending canonical confirmation. `config/_visa-programs.json` GI Cat-2 caveat re-rendered. Facts researched + dual-verified 2026-07-02 (3-agent workflow: treaty status / HMGoG position / local cross-check).
- **Tier-A quarterly refresh — all 16 high-velocity playbooks re-verified and re-stamped 2026-07-03 (issue #291).** Three-wave fleet: 16 grounded researchers (primary sources only) → 16 adversarial auditors (~135 CONFIRM / ~98 CORRECT / 3 REJECT-as-written, zero surviving fabrications) → 16 patch appliers + serial integration. Headline catches now in the corpus: **AU FIRB established-dwelling ban EXTENDED to 30 Jun 2029** (was 31 Mar 2027 — 2026-27 Budget); **PT non-resident IMT surcharge ENACTED** (DL 97/2026, flat 7.5%, tier-1) + the previously-missing IMT Jovem first-buyer exemption; **IE RPZs replaced by permanent nationwide rent control** (1 Mar 2026, RT(MP) Act 2026 — LPT band charges were ~2× understated, now fixed; Vacant Homes Tax added); **UK "mansion tax" (HVCTS, Apr 2028) + property-income +2pp (Apr 2027)** added, s21 abolition past-tense per SI 2026/421; **CH Eigenmietwert abolition FIXED at 1 Jan 2029** (five stale "2028" spots) + Lex Koller tightening consultation; **GR Golden-Visa STR ban** (<60-day lets, €50k fine, Circular 1/2026 pre-reform exemption — playbook + regwatch + `_visa-programs.json` caveat); **FR DPE recalculation** (1 Jan 2026, ~850k homes exit F/G) + micro-BIC €83,600 + auditor-rebuilt DMTO holdout list; **DE KVDG enacted** (BGBl. 2026 I Nr. 138) + GModG kept honestly draft-stage + heat-pump grants BAFA→KfW-458; **ES C. Valenciana ITP cut to 9%/11%** (1 Jun 2026); **NL 2026 rent-control figures** + 30%-ruling corrected to the statute (€38,338 — the government portal carries the typo); **IT IMU seconda-casa base 7.6‰→8.6‰** (pre-2020 stale); **TR Law 7566 Emlak-Vergisi cap** + TCMB KFE un-"deprecated" + 2026 DASK tariff; **US NFIP 30 Sep 2026 cliff** + SALT $40,400 + honestly-hedged foreign-buyer state count; **AE** price-momentum cooling + FTHB programme + SZR/Al Jaddaf freehold conversion + Abu Dhabi rent freeze (auditor-caught) + 2-yr-visa AED-750k floor abolished; **KZ Art. 602 luxury band** (additive formula — proposed wording would have understated ~2×) + CGT 2-yr transition; **AT MieWeG** + the €500k registration-fee exemption EXPIRED 30 Jun 2026. Regulatory-watch: **+61 entries** (incl. a new 🇮🇪 IE section), 4 in-place replacements, 3 watchlist annotations. SKILL.md country-matrix rows corrected in the same commit (AU ban window, CH 2029, US count, TR prints, AE visa floor + AD statute, GR STR ban, PT RMAL/IMT, ES Valencia, IE rent control, KZ base rate, QLD Form 2). Gates: forbidden-phrase 0 · audit-confidence strict 126/0 · sync-docs in sync · markdownlint 0 · regwatch parser 0 malformed. Full run report in `_local/reports/property-deep-dive-update-2026-07-03-tier-a.md`.

- **Aug-1 regulatory-watch revisit wave — all 50 due entries re-verified; 129 grounded corrections across 26 playbooks + 3 shared sections + SKILL.md.** Every entry with `revisit_by 2026-08-01` was re-verified via a 98-agent workflow (one grounded researcher + one independent adversarial auditor per entry, 0 refutes; JE P.93/2025 resolved manually against the gov.je 2026 budget tax summary), then 30 patch agents applied the dual-verified corrections. Highlights: **Florence STR ban narrative inverted** — the "overturned by TAR Toscana, nothing in force" framing was superseded by the in-force Del. C.C. 27/2025 regolamento (upheld Corte Cost. 186/2025 + TAR Toscana sent. 916 & 925/2026, extended to A3/A4 subzones from 21 Jun 2026); **Lisbon RMAL 2nd amendment** (Aviso 29926-A/2025/2, in force 6 Dec 2025 — sale-triggered caducidade now in BOTH absolute + relative containment zones, Misericórdia absolute, thresholds 10%/5-10%); **HCMC STR rule replaced twice** (misdated 26/2024 → 26/2025 → Decision 19/2026/QĐ-UBND eff. 25 Apr 2026, registration-conditional regime); **Mauritius non-citizen resale LTT is a flat 10%** — the widely-cited "higher of 10% price or 30% of gain" formula was dropped from the gazetted Finance Act 2025 (Seventh Schedule Part III); **Israel's war-damage compensator renamed** to the Property Tax Compensation Fund at the Israel Tax Authority (the "Karnit" naming conflated the road-accident fund; + Jun 2025 / 2026 Israel-Iran wars, ~1M cumulative claims, Oct 2025 Gaza ceasefire); **Jersey P.19/2026 HVR cap REJECTED 26 Feb 2026**; **BoJ policy rate 1.0% (16 Jun 2026)**; **VN Starlink licensed Feb 2026** (connectivity.md registry + table flipped); **Miami Beach "Aug 2025 ruling" was a phantom** (restriction in force; only the 2019 fines ruling existed); **CN SAFE Sept-2025 FX reform rescoped** (procedural bank-review easing only — foreign-individual purchase rules unchanged per SAFE's 5 Oct 2025 clarification; unsourced "$50k quota retained" dropped); **Thailand 2026 = full statutory Land & Building Tax rates** (no reduction decree; "renewed annually" corrected to episodic) + new news-tier entry for the Cabinet-approved transfer-fee extension to 30 Jun 2027 (Royal Gazette pending). Sections whose facts were corrected were re-stamped 2026-07-02; next revisit dates staged 2026-10-01 / 2026-11-01 / 2027-01-05 by volatility. Validation: forbidden-phrase gate clean, `audit-confidence.py` strict 126/0, `sync-docs.py --check` in sync, markdownlint clean.

- **Doc/count reconciliation to authoritative sources.** Corrected drifted counts repo-wide: section totals (`--all` runs **40 of 41**; "thirty-eight" / "thirty" / "31" / "24" → 40/41 across `SKILL.md`, `README.md`, `docs/usage.md`, `sync-docs.py`); refresh-tier sizes **15/30/42 → 16/38/72** plus the full tier-membership ISO lists in `updater.md` (now matching `config/_tiers.json`, incl. the `all 87` → `all 126` schedule comment); crime-source registry (44 / 126 → **116**); visa-programs registry (192 records / 13 regions → **201 / 16**); workflow count (3 → **31**) and top-level shared-file count (48 → **72**) — and repaired the `sync-docs.py` regex/template that should own the shared-file count so it auto-syncs going forward instead of silently drifting. Added the omitted 7-flag ownership-diligence group to the README/usage section tables; fixed docstrings/comments pointing at a nonexistent `version-month-guard.yml` and a `--score-only` `results.json`/`cache.json` mismatch.

- ci(version): Version month guard — stop CalVer month drift ([#273](https://github.com/soreavis/property-deep-dive/pull/273)) — by @soreavis

- docs: refresh install.md release-tag refs 2026.05.59 -> 2026.06.0 ([#274](https://github.com/soreavis/property-deep-dive/pull/274)) — by @soreavis

- fix(confidence-audit): recognize national-ccTLD government domains + enforce strict ([#277](https://github.com/soreavis/property-deep-dive/pull/277)) — by @soreavis

- fix(sources): repoint 60 dead source URLs + retire 1 (browser-UA verified) ([#278](https://github.com/soreavis/property-deep-dive/pull/278)) — by @soreavis

- fix: codebase quality sweep — code/CI/docs/consistency bugs ([#279](https://github.com/soreavis/property-deep-dive/pull/279)) — by @soreavis

- fix: close three deferred follow-ups (url-liveness report · dormant ack · config doc) ([#280](https://github.com/soreavis/property-deep-dive/pull/280)) — by @soreavis

- docs: resync auto-generated repo-total line count ([#284](https://github.com/soreavis/property-deep-dive/pull/284)) — by @soreavis

- fix: Aug-1 regwatch revisit wave — 50 entries re-verified, 129 grounded corrections ([#292](https://github.com/soreavis/property-deep-dive/pull/292)) — by @soreavis

- fix(source-verify): remove two self-inflicted TOKENS_ABSENT false-positive classes ([#294](https://github.com/soreavis/property-deep-dive/pull/294)) — by @soreavis

- fix: Tier-A quarterly refresh — 16 countries re-verified + re-stamped (closes #291) ([#295](https://github.com/soreavis/property-deep-dive/pull/295)) — by @soreavis

- fix(ci): feed watcher was reading nothing — replace 3 dead feeds, harden parser ([#298](https://github.com/soreavis/property-deep-dive/pull/298)) — by @soreavis

- fix(source-verify): #289 triage — 103 grounded corrections + nearest-link FP-class checker fix ([#300](https://github.com/soreavis/property-deep-dive/pull/300)) — by @soreavis

- fix(gi): Frontier Treaty in-force — provisionally applied 15 Jul 2026 (closes #275) ([#293](https://github.com/soreavis/property-deep-dive/pull/293)) — by @soreavis

- fix(ci): feed watcher — EP feeds were bot-walled, not dead; re-point to the EP Open Data host ([#309](https://github.com/soreavis/property-deep-dive/pull/309)) — by @soreavis

- docs: correct stale workflow cadences in README + CLAUDE.md file map ([#310](https://github.com/soreavis/property-deep-dive/pull/310)) — by @soreavis

### Fixed

- **The EU-instruments table in `regulatory-watch.md` carried a mis-cited directive, an invented deadline, a repealed instrument and a status column three months stale — every row re-verified against its own EUR-Lex text.** The table is the skill's reference for "which EU rule bites when", and four of its six rows were wrong. (1) **AMLD6 was cited as Directive (EU) 2024/2831** — that is the *platform work* directive (working conditions in platform work, transposition 2 Dec 2026). AMLD6 is **Directive (EU) 2024/1640**; the row also missed both property-relevant tranches of art. 78 — arts. 11, 12, 13 and 15 (beneficial-ownership register access, including legitimate-interest access) due **10 Jul 2026**, and **art. 18, the single access point to real-estate information**, due 10 Jul 2029. The rest of the corpus already cited 2024/1640 correctly (`finance-banking.md`, `notary-forced-heirship.md`), so the error was isolated to this table. (2) **Regulation (EU) 2024/1028's "→ 20 May 2028 (full data-sharing)" milestone does not exist** — the regulation's body contains no 2028 date at all; it has one application date (20 May 2026, penalties provisions included) and an art. 18 evaluation due by 20 May 2031. (3) **Directive (EU) 2018/2002 was repealed** — Directive 2012/27/EU, which it amended, was repealed with effect from 12 Oct 2025 by the EED recast **(EU) 2023/1791** (transposition 11 Oct 2025); the row now cites the recast and says not to cite 2018/2002. (4) **EPBD status was "approaching deadline; member states finalising national plans"** — the 29 May 2026 deadline passed and on **15 Jul 2026 the Commission sent letters of formal notice to all 27 member states** for failing to fully transpose ([DG ENER](https://energy.ec.europa.eu/news/commission-calls-eu-countries-transpose-reinforced-rules-energy-performance-buildings-2026-07-15_en), fetched directly), with two months to respond before a possible reasoned opinion; the earlier art. 17(15) deadline (1 Jan 2025, fossil-fuel-boiler incentives) is now recorded too. Also sharpened: DAC8's three tranches (31 Dec 2025 → apply 1 Jan 2026; 2027 → 2028; 2029 → 2030) replace an unverifiable "Transposed" claim about all 27 member states, and the AI Act's "Phased through Aug 2026" becomes the actual schedule, with the property-relevant hook named — systems evaluating **creditworthiness or credit score of natural persons** (annex III, 5(b)) are high-risk under art. 6(2) and so follow the **2 Aug 2026** general date, not the art. 6(1) route deferred to 2 Aug 2027. The stale `Status (Apr 2026)` header becomes `Status (verified 2026-07-31)`, the section is renamed *EU-wide instruments — transposition and application dates* (it always carried regulations as well as directives, and the two bite differently), and two rules are added: an EPBD claim must now cite the **national** law that exists rather than the untransposed directive, and the AI Act's duties bind the lender operating the scoring system, never the buyer. `SKILL.md` and `README.md` pointers updated to match.

- **Feed watcher opened a fresh issue for the same item on every poll inside the match window.** `WINDOW_DAYS: '3'` bounds how stale an item may be to count as a signal — it does not stop an item already reported yesterday from matching again today, and each poll unconditionally called `issues.create`. Over 16 days that produced **13 issues (#302–#320) carrying 5 distinct items**: one CJEU judgment reported 3× (#302–#304), one EC Daily News 3× (#304–#306), the CRR off-plan RTS 3× (#311–#313) and the EP housing-crisis text 3× (#317–#319), burying the genuinely new signal in re-reads. Added a dedupe step that pulls the bodies of feed-signal issues from the last 30 days (**open and closed** — a triaged item must not return) and drops every match whose link was already reported, keying on the feed link and falling back to the normalised title for link-less items; the issue-create and artifact-upload steps now gate on `has_new`, so a poll where nothing is new stays silent instead of filing noise. New `scripts/dedupe-feed-matches.py` + 6 unit tests, run in the workflow's existing test step; verified against the live repo (today's real 3-item match set → 3 already-reported / 0 new; the same set plus one synthetic item → 1 new, block intact). The misleading inline comment claiming the window prevented duplicates is corrected.

- **Feed watcher red on 2026-07-27 — `curia.europa.eu` moved behind an Azure WAF that answers only browser-shaped User-Agents.** The CJEU press feed was green on 26 Jul and returned **HTTP 403 with a 286 KB Microsoft-Azure-Application-Gateway block page** on 27 Jul, failing the "dead or blocked" gate. The feed itself never moved: a `workflow_dispatch` probe from a GitHub-hosted runner (mirrored locally, 3 reps each) got **403 for `FEED_UA`, 403 for a bare product token and 403 for `curl/8`, against 200 + valid RSS for a desktop Chrome UA and for Firefox** — so unlike the EP case in #309 the runner IP is *not* part of the decision here, only the UA shape is. Re-pointing was not available either: curia is the sole publisher of the Court's press releases and EUR-Lex RSS is account-gated (that is why `ECJ-PR` was moved to this URL on 2026-07-09 in the first place). Fixes: (1) `fetch_and_filter` takes an **optional per-feed UA** (`local ua="${3:-$FEED_UA}"`), and the `CJEU-PR` slot — and only that slot — passes a new `CJEU_UA`: a browser-shaped envelope with **the product token and contact URL appended** (`… Chrome/126.0.0.0 Safari/537.36 property-deep-dive-ci/1.0 (+https://github.com/<repo>)`), which still self-identifies and still answers 200 (8/8 reps with the CI `Accept` header). The three EU feeds keep the honest `Mozilla/5.0 (compatible; …)` form unchanged. The deviation from the no-impersonation rule is documented at the `CJEU_UA` declaration along with what was verified, and it is bounded by curia's own `robots.txt`, which permits the path (one `User-agent: *` group, 17 document-specific `Disallow`s, none matching `/site/rss.jsp`, no crawl-delay) at a once-daily poll. (2) **An unreachable feed now names its HTTP status** — `curl` still writes `%{http_code}` when `-f` rejects a 4xx/5xx, so the annotation, job summary and issue "Degraded coverage" block report `unreachable (HTTP 403)` rather than bare `unreachable` (`000` renders as "no HTTP response — DNS, TLS or timeout"). Today's log named no status, which is what turned a one-line diagnosis into a runner probe. Verified by executing the workflow's own extracted `run:` block against the live feeds — **4/4 healthy** with CJEU-PR parsing 10 items — and again with `CJEU_UA` forced back to the blocked value, confirming the gate still exits 1 and now prints the 403.

- **Feed watcher red on 2026-07-24 — the two European Parliament feeds were bot-walled, not dead, and the job reported them as "retired or soft-404".** `www.europarl.europa.eu` moved behind an AWS WAF that answers with **HTTP 202, an empty body and `x-amzn-waf-action: challenge`**. 202 is a 2xx, so `curl -f` passed the empty response through and `filter-feeds.py` failed on it with `no element found: line 1, column 0`, surfaced as `EP-Press`/`EP-Adopted` "returned no parseable items — retired or soft-404". Both feeds were live throughout (20 and 25 items in a browser); acting on that message would have meant replacing two working primary sources. **The challenge is not UA-solvable from CI**: a `workflow_dispatch` probe from a GitHub-hosted runner got `202` on all five variants tried — the honest `Mozilla/5.0 (compatible; …)` form, a full Chrome UA, `curl/8.7.1`, no UA flag, and Chrome + `Accept-Language`/`Sec-Fetch-*` — so the runner IP range is part of the decision and the URLs are unreadable from Actions whatever headers are sent. Fixes: (1) **both EP slots re-pointed to the EP's own Open Data host**, `data.europarl.europa.eu`, which answers runners normally and serves Atom with English titles and ISO-8601 `<updated>`: `EP-Adopted` → `api/v2/adopted-texts/feed` (~500 entries — same signal as the retired RSS), and the press-releases slot, which has no Open Data equivalent, → `api/v2/procedures/feed` as **`EP-Procedures`** (~410 entries), which surfaces a reform *before* adoption rather than after. Same publisher, still primary-source, feed count unchanged at 4. (2) **An empty 2xx body is now its own diagnosis** — the fetch dumps response headers and a zero-byte feed reports as `empty body (HTTP <code>, x-amzn-waf-action: <action>) — bot wall, not a retired feed; fix the User-Agent before touching the URL` in the annotation, the job summary and the issue's "Degraded coverage" block. The gate still fails the job (degraded coverage stays loud, per #298), message widened to "dead or blocked". (3) `FEED_UA` moved to the job `env` as `Mozilla/5.0 (compatible; property-deep-dive-ci/1.0; +https://github.com/<repo>)` — keeps the product token and contact URL, so the CI identifies itself honestly rather than impersonating a browser, and clears the UA-shape check on hosts where the IP is not also held against it (a bare product token, `somebot/1.0` and even a bare `Mozilla/5.0` are all challenged; documented as **not** a WAF bypass). (4) **Atom now leads the `Accept` list** — the Open Data API content-negotiates strictly and `406`s whenever `application/rss+xml` outranks atom, while both RSS sources ignore the header. (5) `--max-time` 20 → 45 s: the Atom feeds are 250-280 KB and answer in 4-10 s. (6) +1 unit test pinning zero-byte input to exit 2 (27/27 pass). Verified by executing the workflow's own extracted `run:` block against the live feeds — **4/4 healthy**, dates parsing correctly (4 entries dropped as older than the 3-day window), and the new `EP-Procedures` source immediately matching a real item, *Non-financial commercial real estate statistics* (procedure 2025-0052).

- **Source-audit #289 triage — all 242 deterministic tripwires adjudicated by a 244-agent quote-or-abstain fleet; 103 grounded corrections across 46 files; third self-inflicted FP class removed from the checker.** The rolling source-audit issue was first re-baselined by re-dispatching `source-verify.yml` with the post-#294 checker (its 170-row report predated those FP fixes), yielding 165 figure-absent + 84 stale-after-verify flags. Every flag then got an independent verification agent under the `shared/source-verifier.md` contract (verbatim-quote-or-abstain; deep-page finds re-fetched directly, search hits treated as leads only): **80 MATCH / 100 NOT_FOUND / 9 MISMATCH+STALE / 12 PARTIAL / 21 SOURCE_UNAVAILABLE / 22 INDETERMINATE**. Real figure errors caught and fixed with quoted primary evidence: **AD minimum wage 2026 = €1,525.33/month** (Decret 8/2026 — the €1,447 was the 2025 amount mislabelled 2026, further revision Decret 253/2026 flagged); **EC dollarization date 9 January 2000** (BCE — was "9 Sep 2000" in two places) and **Constitución art. 405 inverted** (it *restricts* foreign acquisition in national-security/protected areas — it does not grant "art. 405-406 equality"; regime stays OPEN on the corrected narrower basis); **EC notary arancel** rewritten to the actual Resolución 005-2023 SBU-bracket schedule; **EC FTTH metric** corrected to ARCOTEL's real statistic (fibre = 88.3% of fixed connections, not "~70–85% household coverage"); **AU established-dwelling ban window 1 Apr 2025 – 30 Jun 2029** in the two `finance-banking.md` spots the Tier-A wave missed; **TH inbound-FX documentation threshold USD 200,000** (was USD 50k, superseded 2019, BOT); **NA dollar first issued 15 Sep 1993** (Bank of Namibia — was 21 Sep); **IN non-fossil capacity share 53.7%** (MNRE); **AE ADREC H1 2025 figures** replaced with the actual H1 report's (AED 25 Bn residential, Saadiyat ~33%, Yas ~15%); **TW 房屋稅條例 pcode fixed** (G0340054 was a 2003 transport regulation → G0340102); **VG NBLHL forms revised 2023** (was Sept 2022). Plus **~60 deep-link repoints** (each re-verified live before editing — SAFE US$50k FX quota, NPC lease-cap commentary, Azerbaijan AZN 100k TRP threshold, St Kitts CIU regulation PDFs, …), six Kuwait statute citations moved off the mis-cited statistics bureau to verify-at pointers at the correct authorities, and **DEPRECATED markers** where primary sources genuinely vanished (`energia.ad` parked, MIDUVI + `turismo.gob.ec` domains misrouted to an unrelated agency, ARCONEL resolución page dead, one gov.bm ministerial page 404). 4 rows honestly skipped as unverifiable (unreadable scanned PDFs / unreachable hosts), 12 confirmed already-hedged. Checker: **nearest-link token attribution** — on multi-citation lines every value token was previously attributed to *every* link, making each citation answer for its siblings' figures (~20% of the run's flags); tokens now bind to their nearest link (tie → the following link, matching the `figure ([source](url))` idiom) and roster co-citations owning no figure are dropped: **1,327 → 1,032 checkable claims (−295 pseudo-claims)**, +5 unit tests (105/105 pass). Gates: forbidden-phrase clean, `audit-confidence.py` strict 126/0, `sync-docs.py --check` in sync, markdownlint 0 errors on all 46 touched files.

- **Feed watcher was reading nothing — all three regulatory feeds had been dead, and the job reported green every day.** A GitHub Actions runner-allocation outage put a red X on the 2026-07-09 scheduled run (job never started; not a repo fault), and investigating the re-run's log exposed the real defect underneath: `EU-OJ-L` returned **HTTP 200 with the body "The RSS feed doesn't exist or is no longer valid"** (a soft-404 — EUR-Lex now gates RSS behind a signed-in account), `ECJ-PR` returned **404** (the Court moved its feed to `curia.europa.eu/site/rss.jsp`), and `OECD-Tax` returned **403** (`oecd.org` blocks non-browser clients host-wide — the bare homepage 403s too). Each failure was swallowed by `|| { echo "(failed to fetch …)"; return 0; }` plus a trailing `|| true`, so the watcher printed "(no property-relevant items in last poll)" and exited 0 with **zero feeds actually read**. Fixes: (1) feeds replaced with four primary sources verified live 2026-07-09 — **EC Press Corner**, **EP press releases**, **EP texts adopted** (covers the legislation signal EUR-Lex used to carry) and the **CJEU's current press feed**; (2) the inline parser is extracted to `scripts/filter-feeds.py` (stdlib-only, 26 unit tests in `scripts/filter-feeds-test.py`) and now **matches on title + description** — CJEU titles are bare case numbers (`101/… : null - Opinion of the Advocate General`) whose subject matter lives only in the body, so a title-only filter could never have matched a CJEU item even with a live feed; (3) **CDATA-wrapped titles no longer vanish** — the old `re.sub(r'<[^>]+>', '', title)` treated `<![CDATA[Housing reform]]>` as one tag (`]]>` supplied the closing `>`) and deleted the entire title; (4) double-escaped entities are unescaped before matching; (5) keywords are **word-anchored** and `intellectual property` is excluded, since bare `property` matched an EU copyright/geo-blocking judgment; (6) a **3-day freshness window** stops the daily poll re-reporting the same 10–25 feed items and opening a duplicate issue; (7) a dead feed is now **loud** — a soft-404 or unreachable source annotates the run, lands in the job summary and the issue's "Degraded coverage" block, and **fails the job** in a final step that runs after the issue is opened, so silent coverage loss can't recur. First run against the live feeds returns 4/4 healthy and correctly surfaces the EP's *Special committee on the Housing Crisis in the European Union* (adopted 7 Jul 2026).

- **Source-content audit: two self-inflicted false-positive classes removed from the tripwire (issue #289 triage).** The first `source-verify.py` baseline run flagged 170 `TOKENS_ABSENT` claims; classification showed two structural FP classes generated by the checker itself: (1) **the tracker's own verification stamps** — claim lines like `(2026-05-27 verified, source …)` had the audit date extracted as a salient "figure" the cited government page was then expected to contain (it never will); `mask_inline_stamps()` now blanks the date inside `STAMP_RE` + the new `ASOF_STAMP_RE` ("as of YYYY-MM-DD") forms before value extraction. (2) **Statute-name years** — "Immigration and Protection Act 1956" matched `STATUTE_RE` with the bare year as its only needle, demanding "1956" appear on a landing page; statute values whose needles are all plain years (`19xx`/`20[0-3]x`) are now non-salient, while real statute numbers ("Law 7464", "Ley 2068/2020", "Loi 2024-1039") keep salience. Net effect measured on the corpus: **1,375 → 1,312 checkable claims (−63 pseudo-claims), 0 new claims**, real figures unaffected; +5 unit tests (100/100 pass). Remaining flag classes are genuine tripwires: root/landing-page citations next to statute claims (deep-link or `check-citations.py`'s `linter.root_url_allowlist`) and ~30 real figure-absent candidates to triage on the next scheduled run's re-baseline.

- **Three deferred follow-ups from the quality sweep, now closed.** `url-liveness.yml` restores and uploads the `last-success.txt` timestamp as its own artifact and passes `--last-success-in` to the checker, so the run report's "Previous successful run" line is populated instead of always reading "(none recorded)". `audit-visa-programs.py` gained a `DORMANT` token in `NEEDS_ACK`, making the previously-unreachable `\bdormant\b` acknowledgement pattern live — the Quebec QIIP "effectively dormant" record is now audited (0 unacknowledged). And `config/_url-liveness.json`'s `_comment` now documents that `429`/`999` under `indeterminate_status_codes` are intercepted earlier by the `RATE_LIMITED`/`BOT_BLOCKED` branches in `_classify()` and never reach the indeterminate path (only `403`/`503` do).

- **Code & CI bug sweep (find → adversarial-verify, 58-agent workflow).** A multi-finder audit surfaced and an independent verify pass confirmed a batch of real defects, now fixed: `audit-confidence.py`'s primary-host regex never matched **bare `gov.XX` hosts** (`gov.ky`/`gov.uk`/…), under-counting confidence URLs (same blind spot fixed in `check-citations.py`); `check-counts.yml`'s drift-detect step **aborted under `pipefail` exactly when `sync-docs --check` reported drift**; `transposition-alerts.yml` fired only on an **exact-day threshold match under a weekly cron**, silently missing ~6 of every 7 EU-transposition alerts (now selects the most-urgent unpassed threshold); both `transposition-alerts.yml` and `regulatory-watch-revisit.yml` attributed **combined-header entries to the wrong country** (a Finland reform logged as Sweden — now mirror `regwatch_promotions.py`'s multi-country attribution); `render-match-index.py`'s `no-fx-risk` hard filter **over-claimed for USD-/CHF-pegged jurisdictions** (sg/qa/sa/om/bh/jo/hk/mo/li — now a dedicated `FX_EUR_RISK_FREE` set + regenerated `_match-index.json`); `audit-visa-programs.py` reported acronym programmes as audited while silently skipping them (now an honest audited/skipped split); `next-version.py` accepted out-of-range months; `audit-confidence.py --iso` + a non-matching `--tier` passed green on zero playbooks. Plus robustness/dead-code fixes (claim-window offset, terminal-retry backoff waste, redundant `_classify` recompute, unreachable URL-template guard, unchecked `cell['band']`, cwd-relative regwatch path, annotated-tag mis-pin in `pin-actions.sh`, two dead constants/regexes, a tautological test) and a `ruff` lint sweep. **All 156 unit tests + every audit/render/count gate pass; no country-data values changed.**

- **CI hygiene.** Added `concurrency:` cancel-in-progress groups to the six PR-validation workflows; dropped the unused `pull-requests: write` scope from `pr-validate.yml`; de-duplicated `doc-sync-check.yml`'s scheduled-drift issue (pinned-issue update instead of a fresh issue every week); removed the redundant double `pull_request` / `pull_request_target` trigger from `auto-merge-docs.yml`; guarded the first `feed-watcher.yml` feed fetch with `|| true`; widened `match-index-audit.yml`'s push path filter to include its own test file.

- **Confidence-audit gate recalibrated — it was blind to national-ccTLD governments, then made strict.** `scripts/audit-confidence.py` only counted a source as "primary" when its host matched a `.gov`-family suffix (`.gov` / `.gouv` / `.gob` / `.govt` / `.go.` / `.gv.` / `.gc.`) or the curated allowlist. But many governments use a national ccTLD instead — Germany on `.de` (`dwd.de`, `bafa.de`, `bbk.bund.de`), Switzerland on `.admin.ch`, Luxembourg on `.public.lu`, Belgium on `.fgov.be`/`.belgium.be`, plus the Nordics/Baltics/Balkans, Crown Dependencies and the Dutch & UK Caribbean — so the gate **systematically under-counted their primary sources** and reported **33 of 126 playbooks as below threshold** (incl. flagships `de`=2 and `be`=1), masking any genuine over-claim in the noise. Three-part fix: (1) `is_primary_host` now **suffix-matches** allowlist entries, so one umbrella domain (`admin.ch`, `bund.de`, `public.lu`, `fgov.be`) covers every agency subdomain; (2) **+345 curated registrable government / regulator / statutory / cadastre / central-bank / statistics / met / geological / hazard / court domains** added to `scripts/primary-source-allowlist.txt`, classified per-playbook (grounded in each citation's context) and **independently adversarially audited** (workflow — 55 Opus agents, **384 confirmed / 11 rejected**: state-owned road *operators* `kozut.hu`/`putevi.me`, a public university `setur.fo`, an EU network node `euraxess.fo`, a private-association subsidy programme, and 6 unverifiable domains were correctly excluded; no rejected host is re-admitted by the additions); (3) the lone residual `ch` (9/10) was closed by backing its existing "per ESTV" tax claim with the Federal Tax Administration's own primary source ([estv.admin.ch](https://www.estv.admin.ch/)). Result: **33 → 0 below threshold** across all 126 playbooks. With the corpus clean, `.github/workflows/confidence-audit.yml` **drops `--warn-only` and now enforces strict-mode** (its documented end-state) — a PR that adds an under-sourced playbook or over-claims Confidence now fails CI. No skill-content change beyond the one CH citation.

- **Dead-link sweep — 61 confirmed-dead source URLs repointed/retired (browser-UA verified).** Ran the `url-liveness.py` sweep (8,127 URLs) and triaged its reported-dead set under the standing "re-verify before marking dead" rule. The checker requests with a bot UA, so its 764 reported dead/timeout were **re-verified with a real browser UA** first: 16 were actually alive, 267 indeterminate (403/429/5xx/timeout — not dead), and **394 connection/DNS errors were deferred to CI** because DNS from the run environment proved unreliable in both directions (known-alive gov sites bulk-failed; `dig @1.1.1.1` even returned a false NXDOMAIN for a reachable host) — only resolver-independent HTTP **404/410** responses were trusted. Of the **87 HTTP-confirmed dead**, 9 were extractor artifacts and 16 already carried DEPRECATED markers, leaving **61 genuinely-dead + unmarked** links. Each was repointed to the **current same-authority URL found by research and then independently curl-verified live** (workflow → 60 fixed; the dead nippon.com news article had no equivalent → DEPRECATED with an authority pointer). Root causes were government-site migrations: **States of Jersey** (14 `gov.je/*/Pages/index.aspx` paths), **Guernsey**, **Monaco**, **Macao gazette** (`io.gov.mo` → `dsaj.gov.mo`), **Vietnam GSO** (`gso.gov.vn` → successor `nso.gov.vn`), **France** `impots.gouv.fr`, **Netherlands**, **SingStat**, **IPGP** overseas volcano observatories, plus assorted single pages across 34 files. Per the contract, repoints went to verified same-authority pages (deep where found, the authority's current section/landing where the exact deep page is gone); no replacement was fabricated, and **`Last verified` footers were deliberately not re-stamped** (a URL repoint is not a data re-verification).

## [2026.06.0] - 2026-06-14

### Added

- **Advisor-credential verification — immigration lawyers/advisers + cross-border tax advisers (raw-data-surfaced, gated + built).** A deeper re-dive into the raw `sources.json` (8,258 signals across 360 threads), past the loud already-covered themes, surfaced the **252-signal advisor cluster** ("immigration lawyer to *verify* visa requirements", "everyone's trying to scam me — who's a real lawyer?", "cross-border tax advisor for FATCA / double-taxation"). The skill verified estate agents (`--agent`), notaries (`--notary`) and surveyors (`--surveyor`) but had **no consolidated "verify your immigration lawyer / cross-border tax adviser is regulated" registry**, and unlicensed-adviser / "notario" fraud was **entirely absent (0 files)**. Gated **BUILDABLE-THIN** (SUPPLY × DISTINCTNESS × VALUE) → built as fold-ins (no flag, no new file; section count stays **41**) via grounded-research → independent adversarial audit (4 Opus agents, regulator/primary sources only — **17 rows, 0 dropped; immigration 7/7 CONFIRM, tax 7 CONFIRM + 3 CORRECT**). (1) `shared/scams.md` gains an **8th cross-cutting trap** (unlicensed immigration adviser / "notario" fraud → verify on the regulator's public register: US state-bar / DOJ-EOIR-accredited · UK IAA, ex-OISC rebranded 16 Jan 2025 · CA CICC · AU OMARA · NZ IAA · EU national bar CNB/BRAK/CGAE) + a **6th universal mitigation** (verify by licence number before paying). (2) `shared/cross-border.md` expands the one-line advisor pointer into a **per-country tax-adviser regulator registry** (DE Bundessteuerberaterkammer · FR OEC/CNB · UK CIOT/ICAEW · NL RB/NOB · IT CNDCEC · ES CGE/REAF · US IRS RPO + DOJ enjoined-list cross-check · STEP **TEP** for estates) with public verify-URLs. Audit caught the dead `tax.org.uk` CIOT path, the members-only NOB login URL, and a REAF service-vs-directory mix-up. **Anti-advice rule: track the regulated body + the verify-register — never recommend a named adviser.** `--scams` + `--cross-border` SKILL.md rows enriched.
- **Social-security coordination + pension portability + childcare cost-trap (raw-data-surfaced, gated + built).** A direct dive into the study's raw `sources.json` (360 threads, 5,914 pain/want signals) surfaced 2 buildable gaps the curated FEATURE_MAP had folded into broader buckets; both gated (SUPPLY × DISTINCTNESS × VALUE, 5-unit workflow → independent audit, all cleared) + built as enrichments of existing sections (no flag, no new file). (1) `shared/cross-border.md` § 4 expands from a 2-line pointer into a full **social-security + pension classify-the-system**: **US** 30-country Totalization-Agreement list + the **no-agreement SECA-15.3% self-employed double-charge trap** (FEIE/FTC don't reduce it; DTAs don't see it) + Certificate of Coverage; **EU** Reg 883/2004 single-legislation + A1; **UK** QROPS 25% Overseas-Transfer-Charge (EEA exclusion removed 30 Oct 2024, member jointly liable) + the **frozen-State-Pension** trap (AU/CA/NZ uprating-frozen); **401(k)/IRA-taxed-abroad** treaty framework (residence-state rule + US saving clause; the 12 Mar 2025 HMRC US-UK lump-sum reversal; PFIC/3520 reverse-trap pointer). (2) `shared/demographics.md` gains **Childcare cost + the second-earner trap** — OECD net-childcare-cost (~14% of avg wage; DE ~1% / EE ~0% low) + the *Taxing Wages* 2024 second-earner 34% tax wedge (>75% women), tied to `--work` spouse-work-rights. **Framework-only — never a per-person liability/eligibility verdict.** Audit corrected US entry-into-force dates, the QROPS member-liability, the US-UK lump-sum saving-clause override, and dropped an unsourced part-time-women stat. `--cross-border` + `--demographics` SKILL.md rows enriched; section count stays **41**.

- **Relocation-study gate — final 2 REJECT-OVERLAP fold-ins (gate now 100% actioned) + 2 stale-line tidies.** The gate's two REJECT-OVERLAP items had thin net-new slices, now folded (no flag, no new file): (1) `shared/visa-programs.md` gains a `## Golden-visa price distortion` note — the threshold-clustering **"visa premium"** trap (price the asset against comparable *non-qualifying* stock, not against the threshold), anchored to the **PT Mais-Habitação** RE-route abolition + **GR Law 5100/2024** threshold rise (both reform rationales = housing-price pressure), hedged (magnitude not reliably published — never a fabricated % premium) + cross-linked to `--price`; (2) `shared/compare.md` § Limitations gains a **livability discoverability pointer** (no-composite side-by-side via `--compare` / `--demographics` / `--match`) with an explicit **"no scored livability index"** refusal. Section count stays **41**. Also tidied 2 stale ROADMAP reconciliation lines (Batch D Phase C + `--seller-withholding` / `--title-monitoring` still read as "queued" when all three had shipped). With this **both 2026-06-14 feature gates are 100% built — the ROADMAP feature backlog is exhausted.** Docs only.

- **Regulatory-watch revisit refresh (health-report-driven) — 5 due/overdue entries re-verified.** A full health-report run (decay matrix **126/126 HIGH, 0 off-cadence**) surfaced **1 overdue + 4 due-soon** regwatch revisits; all 5 were re-verified via a workflow (grounded research → independent adversarial audit; 0 fabrications — the audit defaulted to STILL_PENDING / inferred where no primary outcome statement existed) and refreshed (`verified_date` → 2026-06-14, `revisit_by` bumped). **🇲🇻 MV** (overdue) — the 16th-Tourism-Act discounted resort lease-extension window **LAPSED on its 5 Jun 2026 end date** (closure inferred); fixed the `mv` playbook's stale **"USD 5M flat" default-extension figure** — USD 5M was the *discounted-window* rate, post-window default reverts higher (~USD 10M reference) — across the `--tax`/`--exit` lease lines. **🇹🇷 TR** — 5G **ENACTED** 1 Apr 2026 (state ceremony 31 Mar; all 81 provinces, 3 operators) → `connectivity.md` TR one-liner + reform-calendar updated to past-tense, entry downgraded tier-3 → 4. **🇳🇱 NL** — Box 3 still forfait + *tegenbewijsregeling* for 2026 (structural *Wet werkelijk rendement* passed the Tweede Kamer 12 Feb 2026, still pending in the Eerste Kamer, targeted ~2028); corrected the misleading "actual-return effective 2026-01-01" framing. **🇬🇷 GR** — Cyclades 20-30% bed-cut + Mykonos STR freeze remains a **draft JMD, unsigned** (consultation closed late May 2026); entry marked not-yet-enacted. **🇺🇸 US** — Zillow **MAINTAINS** its First-Street climate-score de-emphasis (re-verified, not restored). Docs/maintenance only.

- **ROADMAP status sync (docs only).** Marked the relocation-study demand-map **§ Status** as fully shipped — all eight build-board items (#255–262) landed as blocks / modes / sub-extensions, section count held at **41**, extensions 13 → 15. The gate's candidate rows + the decision log already recorded each ship; this corrects the one stale "Not greenlit" summary line. With both feature-candidate gates now fully built (relocation-study demand-map + mover-experience), the ROADMAP feature backlog is exhausted — only the normal `--update` maintenance cadence remains.

- **Mover-experience fold-ins #2 — `--cost-of-living` + `--community` (gate #252, the dataset-backed pair; the four are now ALL shipped).** Both folded in (no flags, no new files) after light dataset-verification workflows (grounded research → independent audit; 9 datasets verified, 0 fabrications). (1) `shared/macro.md` gains a `## Cost-of-living — net-of-tax country index (sourcing + the city-vs-home cap)` block + a `--criteria=cost-of-living` row in `compare.md`: classify-the-system 🟢 **US BEA RPP** (sub-national, 2024) / 🟡 **UK ONS RRCPL** (2016 one-off) / 🟠 **country-level net-of-tax** (OECD Taxing Wages 2026 ÷ Eurostat PLI 2024 / World Bank ICP 2021) / 🔴 none; the net-of-tax method (net take-home ÷ price level) is the orthogonal slice neither `--retirement` nor `--digital-nomad` carries; **city-level hard-capped to the US, Numbeo/Expatistan fenced as crowdsourced** (never a verdict basis); the `_match-index` criterion is left reserved (not slot-filled). (2) `shared/demographics.md` gains a `## Integration environment (policy + lived-outcome gap)` block: **MIPEX** policy band (⚠️ the workflow caught that the **2025 refresh covers only EU-27 + Canada**; for the rest the latest is still **MIPEX 2020 / 2014–2019** — date-stamp accordingly) + **OECD/EU Settling-In 2023** immigrant-vs-native employment-rate gap; loneliness / churn / "expat happiness" explicitly fenced off (**InterNations Expat Insider** confirmed a self-selected non-probability survey). No new flags — section count stays **41**. Verified 2026-06-14.

- **Mover-experience fold-ins #1 — `--settling-in` ordered-dependency spine + `--fit` structural-exit block (gate #252, 2 of 4 SHIPPED).** Both folded in (no new flags, no new files) — composition of existing content. (1) `shared/relocation.md` gains a `## Settling-in sequence — what unlocks what` ordered-dependency spine: the 5-step chain (residency/address registration → tax-ID → bank → public-healthcare → utility) with each step's gating + the bank⇄address⇄utility Catch-22 + named workarounds (consular tax-ID, pre-arrival cross-border IBAN, notary *attestation d'achat*) — presented as an **ORDERING, never a day-numbered calendar** (lead times = sourced ranges; statutory maxima labelled as such). (2) `shared/integrity-remorse.md` gains a `## Why lifestyle moves END — the structural drivers (not a statistic)` tail block: 4 documented structural exit drivers (residency-non-renewal · healthcare/care-need-with-age · exit-tax+FX/recapture shock · thin-resale-liquidity lock-in) each converted to a pre-offer self-check, under a **standing refusal of any "X% leave in N years" rate** (the viral "73% leave Spain" = one non-peer-reviewed sample; no authoritative retention dataset, OECD 2024). No new flags — section count stays **41**. Verified 2026-06-14.

- **`--work` spouse-work-rights + credential recognition — new 15th sub-section extension (relocation-study board #6, SHIPPED).** New `shared/work-rights.md` (gated on `--visa`) answering the two questions a relocating professional family faces: **(1) can my spouse work** — 🟢 immediate, unrestricted (DE family-reunification · FR *talent-famille* · ES RD 1155/2024 · IT *motivi familiari* · NL partner-of-kennismigrant · IE post-2019 · UK Skilled-Worker dependant · AU secondary applicant) vs 🟡 conditional (US split by visa class — L-2/E immediate, H-4 only with an approved I-140; CA Family Open Work Permit, **narrowed to spouses 21 Jan 2025**); and **(2) how credential recognition works** — EU automatic-7 + general system via REGPROF (DE Anerkennung/BIBB · FR · ES · IT D.Lgs.206/2007 · NL · IE) vs **UK post-Brexit per-regulator** (no EU automatic recognition) vs **US state-licensing** (no federal) vs **AU occupation-specific assessing authorities** vs **CA provincial regulators**, each with its ENIC-NARIC node (ZAB · CIMEA · Nuffic · QQI · UK ENIC · USNEI · WES/IQAS). **The hard rule**: it tracks the system + names the authority and **NEVER** predicts whether a qualification will be recognised (per-applicant national adjudication). Built via a workflow — 10 systems × grounded research → independent adversarial audit (20 Opus agents; immigration-authority / EU REGPROF / national-recognition-portal sources only): all 10 kept (**7 CONFIRM + 3 CORRECT** — ES org-unit name, NL profession label, IE database attribution; confirmed the post-Brexit UK loss + the 21 Jan 2025 CA narrowing). Honest tail: ~116-country remainder → "verify at the national recognition body / licensing board". **Sub-section extensions 14 → 15**; no new flag — section count stays **41**; count cascade applied (README tree + skill-file count 198→199, CLAUDE.md file-map, SKILL.md `--work` row). Verified 2026-06-14.

- **`--cross-border=timing` tax-residence day-counter (relocation-study board #4, SHIPPED).** New `### Day-counter mode` subsection in `shared/cross-border.md` (under the Tax-residence framework) that tallies the buyer's days **per the destination's own counting method** and **classifies the split-year system** — 🟢 STATUTORY (UK SRT FA2013 Sch45 · IE s822 TCA1997 · DE §2(7) EStG) · 🟡 PARTIAL-YEAR / ADMINISTRATIVE (US dual-status SPT · NL *M-aangifte* · PT 2015 reform · AU s6(1) · FR administrative) · 🔴 NONE / whole-year (ES Art. 9 LIRPF · IT Art. 2 TUIR, no *frazionamento*) — across 10 destinations, with the per-country count method (present-at-midnight vs any-part-of-day vs US 3-yr weighted). **The honest tail dominates**: it counts days + classifies the system and **never** emits a residency verdict, a "non-resident at 182 days" claim, or a liability figure — because ties / permanent home / centre of vital interests / status (US green-card + citizenship) / NL "durable personal bond" can make a person resident on *far fewer* days; routes to cross-border counsel in BOTH jurisdictions. Built via a workflow — 10 systems × grounded research → independent adversarial audit (20 Opus agents; statute / revenue-authority sources only): all 10 kept (**8 CONFIRM + 2 CORRECT** — IT/PT supporting-detail fixes; UK confirmed verbatim against FA2013 Sch45 + HMRC manual). Realises the "Leaving this country" timing stub `cross-border.md` promised. **`=timing` sub-mode, NOT a new flag** — section count stays **41**; `--cross-border` catalog row enriched. Verified 2026-06-14.

- **`--tco` move-budget mode — the one-time relocation cash event (relocation-study board #5, SHIPPED).** New `## Move budget` block in `shared/tco-calculator.md` composing the one-time relocation lump sum buyers under-budget, kept SEPARATE from the 30-year carry: closing costs (§ A, sourced) + visa blocked-deposit (`finance-banking.md` — DE *Sperrkonto* €11,904, flagged as a **liquidity-lock released monthly, NOT a sunk cost**) + ToR customs exposure (`relocation-household-customs.md`) + international shipping (est. ~US$2,500–19,000, 3 FIDI/IAM-accredited quotes) + pet (€500–6,000) + vehicle delta (€2k–25k) + onboarding friction (€1k–3k, `relocation.md` mitigation #7) + runway (user_months × monthly burn). Presented as a **3-scenario band** (lean ~€6k / central ~€18k / heavy ~€45k+) per the tco anti-hallucination rule; the sold-vs-shipped split is the dominant lever. **Composition build — no research workflow** (every line item is already shipped + audited in #256 / #259 / tco § A / relocation.md, so a research wave would add no primary sourcing). No new flag — section count stays **41**; the `--tco` catalog row is enriched. Verified 2026-06-14.

- **`--relocation` household-goods transfer-of-residence (ToR) customs — new 14th sub-section extension (relocation-study board 🥇, SHIPPED).** New `shared/relocation-household-customs.md` (+ a 4th universal-contract item + an 8th cross-cutting trap in `relocation.md`) classifying the transfer-of-residence customs relief that waives import duty + VAT/GST on *used* household goods on a permanent move — the €10k-container-surprise gap. Classify-the-regime spine across **10 primary-sourced systems** on five levers (prior-residence-abroad · owned-&-used · import deadline · non-disposal lock · named form): EU (Reg 1186/2009 — ≥12mo / ≥6mo / 12mo / 12mo; DE Zoll 0350 · FR Cerfa 10070\*03) · UK ToR1 · US CBP 3299 (no VAT, used ≥1yr) · AU B534 (UPE) · NZ NZCS 218 · CA BSF186 · CH Form 18.44 · AE Dubai Customs · SG GST relief · JP C-5360. Honest tail: duty + VAT on non-qualifying goods (single rates CH 8.1% / SG 9% / AE 5%+5% / AU 10% / NZ 15% GST / US none / EU-UK per-MS VAT) + an est. container-shipping band (get 3 FIDI/IAM-accredited quotes — no named movers) + 🔴 verify-with-national-customs for the long-tail. Vehicles + alcohol + tobacco + new/commercial goods excluded everywhere (vehicles = separate regime). Built via a workflow — 10 regimes × grounded research → independent adversarial audit (20 Opus agents; customs-authority / statute / official-form sources only): all 10 kept (**2 CORRECT + 8 CONFIRM** — the audit corrected the FR form revision to *Cerfa 10070\*03*, confirmed the US has no prior-residence test or resale lock, and flagged the EU duty-vs-VAT two-instrument split). **Sub-section extensions 13 → 14**; no new flag — section count stays **41**; count cascade applied (README tree + skill-file count, CLAUDE.md file-map, SKILL.md `--relocation` row + tree). Verified 2026-06-14.

- **`--currency` transfer-cost + `--agent` rental-bidding fold-ins (relocation-study quick-wins #4 + #5, SHIPPED — closes the quick-win bucket).** Two surgical fold-ins. (1) `shared/currency.md` gains a `## Practical transfer cost — the FX margin on moving your money in` note: the exchange-rate spread a retail bank bakes into a six-figure transfer can dwarf the visible wire fee — compare the all-in landed amount and price the move via **specialist FX brokers / multi-currency accounts** + forward contracts (anti-brand: no consumer-fintech brand names per CLAUDE.md), cross-linked to *Hedging market depth*. (2) `shared/agent.md` gains cross-cutting trap #6, *"advertised rent is a floor, not a ceiling"*: in supply-tight markets (NL Randstad · DE Berlin/Munich · IE Dublin) the asking rent is an opening bid, but rent regulation can claw an over-ask back — NL *Wet betaalbare huur* (in force 1 Jul 2024, WWS points cap + *Huurcommissie*), DE *Mietpreisbremse*, IE Rent Pressure Zones — so verify the legal-max before bidding up. **No new flags — section count stays 41.** With these the relocation-study quick-win bucket is closed (the 6th, `--insurance` visa-mandated health band, was triaged SKIP — property-peril file, already served by `digital-nomad-healthcare.md`). Verified 2026-06-14.

- **`--schools` enrollment sequence for relocating families (relocation-study quick-win, SHIPPED).** New universal-contract item 7 + a `## Enrollment sequence for relocating families` block in `shared/schools.md` covering the public-system path a relocating family actually walks: intake window, the **mid-year-entry route** (a right under compulsory-education law, not the annual cycle), **foreign-credential placement** (by age ±1–2 yrs + assessment almost everywhere — no school-grade equivalence body except PT's *equivalência* regime), and the **newcomer host-language entitlement**. Classify-the-system spine on the decision-relevant axis: 🟢 **statutory** named framework (FR UPE2A · DE Willkommens-/Vorbereitungsklasse · ES LOE Art. 79 *aulas de enlace* · PT PLNM · NL ISK) vs 🟡 **discretionary / space-limited** (IE EAL provisional · UK school-led, no ring-fenced grant · AU NSW New Arrivals Program, some temp-visa children fee-charged) — the over-assumed trap. Plus a "raising kids in a non-native language" decision note (the secondary-exam-cliff risk). Built via a workflow — 8 countries × grounded research → independent adversarial audit (16 Opus agents; education-ministry / statute / Local-Authority-code sources only): all 8 kept (2 CORRECT + 6 CONFIRM — the audit fixed the FR circular NOR reference and the ES LOE article granularity, and confirmed the statutory-vs-discretionary split). **No new flag — section count stays 41**; the `--schools` section gains an 8th universal-contract item. Verified 2026-06-14.

- **`--finance` visa-linked blocked-deposit / proof-of-funds (relocation-study quick-win, SHIPPED).** New `## Visa-linked blocked-deposit / proof-of-funds` block in `shared/finance-banking.md` drawing the decision-critical line between a **blocked account** (funds *frozen*, released in monthly tranches — a real liquidity lock) and a one-time **proof-of-funds** (balance only *shown*). Classify-the-system spine across 8 primary-sourced regimes: 🟢 **frozen** — 🇩🇪 DE *Sperrkonto* (the canonical: students €11,904/yr = €992/mo BAföG-linked, *Chancenkarte* €1,091 net/mo; providers Fintiba/Expatrio/Coracle/Deutsche Bank) + 🇳🇿 NZ ANZ-run Funds Transfer Scheme (conditional on six source countries); 🟡 **shown** — 🇦🇹 AT (§293 ASVG Richtsatz €1,308.39/mo single, double for the pensioner permit), 🇨🇭 CH (CHF 21,000/yr student), 🇫🇷 FR (visitor €1,477.93 net/mo = net SMIC; student €615/mo), 🇨🇿 CZ (15× + 2× subsistence min → 46,950 CZK base), 🇮🇪 IE (€10,000 student), 🇦🇺 AU (AUD 29,710 + dependants). Built via a workflow — 8 countries × grounded research → independent adversarial audit (16 Opus agents; immigration-authority / foreign-ministry / statute sources only; default-to-REJECT): all 8 kept (3 CORRECT + 5 CONFIRM — the audit corrected the CH Geneva figure for the LASLP supersession of RIASI, AU's stale 60k/70k income alternative → AUD 87,856/102,500, and the NZ mechanism → the ANZ-run FTS, not a "FundView" scheme). Honest tail: most of the corpus has no fixed published floor (consular "sufficient means" discretion); EU/EEA/Swiss free-movers exempt. **No new flag — section count stays 41**; the `--finance` banking extension is enriched. Verified 2026-06-14.

- **`--visa` reality-check — the most-mythologised financial floors (relocation-study DO-NOW quick-win, SHIPPED).** New hand-written `## Reality-check` section in `shared/visa-programs.md` contrasting the **folklore figure** foreign buyers budget to with the **primary-sourced real all-in floor**, for the 8 most-mythologised residence/visa programmes: 🇳🇱 NL DAFT (the "€4,500" is *locked* business capital, not spendable → ~€6,700–7,250 yr-1 all-in), 🇪🇸 ES Non-Lucrative (€28,800 proven upfront + €7,200/yr per dependent + private insurance + no-work rule — not a €2,400/mo salary), 🇨🇷 CR Rentista (the $2,500/mo must be a ~US$60k bank-guaranteed deposit, not a payslip), 🇲🇾 MY MM2H Silver (USD 150k FD + compulsory MYR 600k property + 8% foreigner stamp duty ≈ USD 310k — not "cheap"), 🇵🇹 PT Golden Visa (NO real-estate route since *Lei 56/2023*; cheapest €200k donation / €500k fund), 🇬🇷 GR Golden Visa ("€250k" dead — €800k Attica/Thessaloniki/Mykonos/Santorini/islands or €400k elsewhere, *L.5100/2024*), 🇦🇪 AE Golden Visa (AED 2M property → ~AED 2.09M all-in; "any property" is the separate 2-yr visa), 🇹🇭 TH LTR (USD 1M net worth + USD 500k deployed into Thai assets — not the THB 800k retirement visa). Built via a workflow — 8 candidates × grounded research → independent adversarial audit (16 Opus agents; primary gov/embassy/statute sources only; default-to-REJECT): all 8 returned confirmed, material, decision-changing gaps (7 CONFIRM + 1 CORRECT — the audit killed the hypothesis that PT's cheapest route is the €500k fund, re-based it to the €200k donation, and refined the MM2H/AE/GR figures). The 8 affected `config/_visa-programs.json` records each carry a terse ⚠️ pointer in `caveats`, re-rendered into the master tables. **No new flag, no schema change — section count stays 41**; the `--visa` section is enriched. Verified 2026-06-14.

- **ROADMAP — relocation-study demand-map gate (docs only, no version bump).** Feasibility-gated the next tier of candidates from the Reddit relocation-property study's `FEATURE_MAP.md` (6 recommendations + 6 quick-wins, beyond the 4 mover-experience candidates) via a 7-analyst **SUPPLY × DISTINCTNESS × VALUE** pass with primary-source web probes. **Verdict: still 0 new flags** — 1 BUILDABLE (household-goods + transfer-of-residence customs → new `shared/relocation-household-customs.md`; Council Reg 1186/2009 + UK ToR1 + US 19 CFR 148) + 3 BUILDABLE-THIN extensions (tax-residency-timing day-counter → `--cross-border`; work-rights + credential recognition → `--work`, EU REGPROF + ENIC-NARIC; move-budget → `--tco` mode) + 2 REJECT-OVERLAP fold-ins (ownership/"buying ≠ residency" — already shipped, only the golden-visa price-distortion line is net-new; livability — already in `--compare`/`--match`, no scored index). Quick-wins: DO-NOW `--visa` floor reality-check (data already in `_visa-programs.json` `min_threshold`); BUILDABLE `--finance` blocked-deposit + `--schools` enrollment-sequence; FOLD-IN `--currency` transfer tip (anti-brand — no Wise/Revolut) + listings bid-on-rentals; SKIP `--insurance` health band. Build-ready specs (named datasets + spine + honest tail + target file + a value×cleanliness build order) recorded in `ROADMAP.md` § Feature candidates — relocation-study demand map. Section count stays **41**; not greenlit. Same-day follow-up: consolidated the gate's 6 candidates + 6 quick-wins into a single ranked **§ Build priority (consolidated)** board (🥇 household-goods + ToR customs → 🥈 `--visa` floor reality-check → 🥉 `--finance` blocked-deposit → tax-timing → move-budget → work-rights, `--schools` in parallel) and reconciled the Status-line build order to match — presentation only, no candidate/verdict/spec changes.

- **ROADMAP — mover-experience feature-candidate gate (docs only, no version bump).** Feasibility-gated 4 proposed sections (`--cost-of-living` · `--settling-in` · `--community` · `--fit`) via a 4-analyst **SUPPLY × DISTINCTNESS** pass with primary-source web probes (the gate that reshaped `--climate-future` + closed Tier-3). **Verdict: all 4 → FOLD-IN, 0 new flags** — each pitch's distinguishing payload (city-vs-home cost ratio / day-bucketed 30/60/90 calendar / loneliness-churn signal / "X% leave in 24 months" retention rate) is **not primary-sourceable** (Numbeo/Expatistan crowdsourced · InterNations self-selected survey · single n=862 Medium post) = fabrication pressure, while the clean slice already has a home. Recorded as **build-ready fold-in specs** in `ROADMAP.md` § Feature candidates — mover-experience gate (named primary datasets — OECD Taxing Wages + Eurostat PLI + US BEA RPP for cost-of-living; MIPEX 2025 + OECD "Settling In 2023" for community; OECD 2024 return-migration for fit — + the classify-the-system spine + honest-tail design that keeps each anti-hallucination-clean). Targets: `_match-index` reserved `cost-of-living` criterion · `relocation.md` ordered-dependency spine · `demographics.md` "Integration Environment" block · `integrity-remorse.md` structural-exit tail. Section count stays **41**; not greenlit.

- **`finance.md` brought current to 126 countries (plugin 2026.05.70 → 2026.05.71).** The universal `--finance` doc (`shared/finance.md`) has per-country mortgage tables but was **frozen at the 87→103-country era** — missing the 23 post-103 jurisdictions: 6 Crown Deps / DK territories (`je`/`gg`/`im`/`gi`/`fo`/`gl`) · 4 NL Caribbean (`aw`/`cw`/`sx`/`bq`) · 4 Caribbean UK-OT (`ky`/`bm`/`tc`/`vg`) · 4 CBI (`kn`/`ag`/`lc`/`gd`) · `na`/`bw`/`pk`/`tt`/`vu`. Backfilled all 23 as grounded per-country table rows — extracted from each country's playbook `--finance` + `--currency` sections via a 4-agent grounded-extraction workflow, then **independently fabrication-audited** against the playbooks (grounding-only repairs). Added two new region tables (**Crown Dependencies & Territories**, **Pacific**) + 13 rows to the Caribbean table + `na`/`bw` (Sub-Saharan Africa) + `pk` (South Asia). **Anti-hallucination throughout**: where a jurisdiction has no domestic non-resident mortgage market the row says so honestly ("cash market" / "data not publicly available — verify with lender") rather than inventing an LTV/rate — KY/TC/VG/BM/LC/TT/VU/GL are cash/leasehold markets with no published non-res series; NA/BW/PK carry the playbooks' explicit "est., advisory — NOT a BoN rule" / "confirm with bank" / "set per bank" hedges; currency regimes stated exactly (KYD 1.20 · XCD 2.70 · AWG/XCG 1.79 · USD-official for tc/vg/bq · BMD 1:1 · VUV basket peg · NAD 1:1 ZAR · BWP crawling-band basket · PKR managed float, NOT a peg · TTD managed float + FX rationing · GBP-parity Crown Deps · DKK/ERM-II). The stale line-3 enumeration ("87 supported countries (44 originals + Tier-1…Tier-5)") → "126 supported countries; per-country tables authoritative"; Status footer + Snapshot re-dated 2026-06-13. This **resolves the `finance.md` out-of-scope flag** noted in the cross-cutting backfill entry below (correction: finance.md DID have per-country tables — it was stale, not merely universal-logic). NOT a new flag — section count stays 41.

- **Batch D Phase C — cross-cutting backfill wave (closes the deferred follow-up)** (plugin 2026.05.69 → 2026.05.70). Backfills the 5 Phase-C countries (`na`/`bw`/`vu`/`tt`/`pk`) into the cross-cutting layer that the playbook PR (#249) deliberately deferred: **all 28 maintained per-country shared-section tables** (`sanctions` · `title-monitoring` · `scams` · `inherited-noncompliance` · `latent-defect` · `squatter` · `surveyor` · `finance-banking` · `finance-crossborder` · `exit-seller-withholding` · `currency` · `agent` · `permits` · `scams-postcompletion` · `integrity-remorse` · `notary-forced-heirship` · `risks-build-quality` · `renovation` · `relocation` · `digital-nomad-healthcare` · `mains-reliability` · `connectivity` · `language` · `schools` · `crime-sources` · `property-management` · `rental-yield-delta` · `auction-registry`) **+ the multi-file `exit` section** (`exit.md` region table/heatmap + `exit/sub-saharan-africa.md` [na/bw] + `exit/asia-pacific.md` [vu] + `exit/caribbean.md` [tt] + `exit/south-asia.md` [pk]); **`config/_visa-programs.json`** (5 new records — `vu` DSP/CBI with the EU-Schengen-revocation caveat, `na`/`bw` residence/CBI hedged `OPEN — VERIFY` / `ANNOUNCED — NOT YET LIVE`, `tt` `NO formal RBI/CBI`, `pk` LTR-2025 — rendered into `visa-programs.md`); the section-contract + catalog **scope-count flips → 126** (`sections.md`, SKILL.md shared-file list, `transport-noise`, `remote`, `notary-deed-anatomy`, `compare.md`); and **5 regulatory-watch reform seeds** (NA Land Bill / TT Property Tax 3% ARV / BW Citizenship (Amendment) Bill / VU EU Schengen revocation / PK 27th-Amendment FCC + Finance-Act cadence). Built on a **grounded-extract → independent fabrication-audit** pipeline (29 sections × 2 stages = 58 Opus agents; each extractor reads the file's existing format + the shipped playbooks, each independent auditor re-verifies the 5 new entries against the playbooks and repairs grounding-only). **17 sections clean, 12 fixed, 0 major issues, 0 surviving fabrications** — the independent audit caught ~15 fabrications-by-false-attribution / slot-fills / dropped-hedges and repaired every one: `auction-registry` TT invented FIA s.8(1)(c)/(d) + a "1-year licence-free window" removed; `risks-build-quality` PK invented "M7.6" magnitude dropped to the playbook's "~73,000 deaths"; `permits` PK "no national building code" false-attribution removed; `renovation` BW slot-filled "~71% tribal / ~3–5% freehold" percentages → the playbook's qualitative wording + NA introduced "National Building Regulations" statute hedged; `schools` TT invented "education is a priority forex category" + "decade-long" → the playbook's "multi-year"; `mains-reliability` BW invented "drought ~every 5 yrs" cadence → "recurrent drought"; `language` PK over-claim re-grounded; `integrity-remorse` / `exit-seller-withholding` / `relocation` / `rental-yield-delta` mis-attributed sources / statutes / authority URLs + dropped est-hedges corrected. **NOT a new flag — section count stays 41.** Cross-cutting coverage denominators bumped 121 → 126; the legacy-denominator gap deferred from Phase B is now **fully closed** (the corpus is internally consistent at 126). `finance.md` — a stale legacy universal-logic doc frozen at "87 countries" (illustrative region examples, never a maintained per-country table) — is explicitly left out of scope and flagged for a separate cleanup.

- **Batch D Phase C — 5 medium-signal country playbooks (country count 121 → 126)** (plugin 2026.05.68 → 2026.05.69). Standalone playbooks for **`na` Namibia** + **`bw` Botswana** (→ `africa`), **`vu` Vanuatu** (→ `apac`), **`tt` Trinidad & Tobago** (→ `caribbean`), **`pk` Pakistan** (→ `south_asia`); all Tier-C (`config/_tiers.json`). Each ~640-760 lines, mirroring the `bb` Barbados structure (critical-framing blockquote + country profile + foreign-buyer-eligibility + 13 `--` sections + cost benchmarks + caveats + verification authorities + Status footer). Built on a **research → independent fabrication-audit → author → fabrication-audit** pipeline (20 Opus agents; per-country research capped at ~16 primary-gov web calls → an independent adversarial audit that re-verified every load-bearing claim from fresh primary sources → authoring → a final 8-check anti-hallucination gate with grounding-only repair). **Load-bearing facts locked from the Phase-A audit + re-verified**: **TT's final court is the UK Privy Council (JCPC), NOT the CCJ** (T&T acceded only to the CCJ *Original* Jurisdiction); **VU is leasehold-only** (1980 Constitution Ch.12 Arts 73–75 vest all land in indigenous custom owners — no freehold; foreigners hold ≤75-yr registered leases under Land Leases Act [Cap 163]); **PK land records are PROVINCIAL** (no national cadastre; Punjab PLRA most digitised); **BW tribal land** (Land Boards / Tribal Land Act) is closed to non-citizens (scarce freehold is the foreign route); **NA urban/residential freehold is NOT banned** — the one enacted restriction is agricultural farmland under the Agricultural (Commercial) Land Reform Act 6 of 1995 s.58, and the sweeping consolidating Land Bill re-tabled Oct 2025 is flagged a BILL, NOT enacted. **Audit corrections folded in**: BW transfer duty is **5% citizen / 10%+15% non-citizen** (Transfer Duty (Amendment) Act 2023 — the widely-cited flat 30% is STALE); TT property tax **3% of ARV** confirmed operative (collection began 2024; a lone aggregator's "2%" was uncorroborated and rejected); a misattributed Karachi all-causes fatality figure was removed from the PK street-crime claim; a single VU GDP-growth number was replaced with a sourced gov/IMF/ADB range; VU's CBI (DSP donation, ~USD 130k order-of-magnitude, NOT property-linked) carries the **EU permanent visa-free-Schengen revocation 12 Dec 2024** (Reg (EU) 2025/11). Anti-hallucination throughout — every numeric fact dated + primary-sourced, currency regimes stated exactly (NAD 1:1 ZAR CMA / BWP crawling-band basket / VUV basket peg / TTD managed float + FX rationing / PKR managed float), genuine gaps marked "data not publicly available — verify at <authority>"; 0 forbidden phrasings; all 5 **Confidence: MEDIUM** (honest for medium-signal jurisdictions). Country-count cascade 121 → 126 across README (matrix auto-rendered from `config/_regions.json` + heading/anchor/prose/file counts), `plugin.json`, `SKILL.md` (description + 5 matrix rows + maintenance scope + tree; stale Tier-C count corrected 49 → 72), `CONTRIBUTING.md`, ISSUE_TEMPLATE, `regulatory-watch.md`, `updater.md`, `docs/`. **Cross-cutting backfill DEFERRED to a follow-up wave** (mirrors Phase B → Waves 1–3): the 5 are NOT yet added to `config/_visa-programs.json`, `shared/compare.md` / `config/_match-index.json`, `crime-sources.md`, `finance.md`, or the other ~28 cross-cutting shared-section tables — each playbook is self-contained (own `--visa` / `--crime` / `--finance` sections), and bumping shared coverage now would overclaim (legacy-denominator rule), so those shared-file coverage counts deliberately stay at 121. Regulatory-watch seeds (NA Land Bill, TT property-tax bill, BW Citizenship (Amendment) Bill, PK 27th-Amendment FCC + Finance-Act cadence, VU EU revocation) also follow in the backfill wave. **The country-side ROADMAP backlog is now exhausted** — only the normal `--update` maintenance cadence remains.

- **ROADMAP Tier-3 bucket CLOSED — 2 extensions built + 2 standalone candidates rejected (plugin 2026.05.67 → 2026.05.68; NOT new flags — section count stays 41).** The last 4 ROADMAP medium-novelty candidates were **feasibility-gated first** (4 parallel analysts scored SUPPLY = primary-source coverage × DISTINCTNESS = overlap vs existing sections, the same gate that reshaped `--climate-future`); 2 cleared and were built via a research → **independent adversarial audit** → finalize pipeline; 2 were rejected as standalone sections. **Built: (1) transport-noise** → `shared/transport-noise.md` (~225 lines, a `--traffic` extension — flight / rail / wind-turbine acoustic exposure the count-derived road-noise verdict lacked): a **classify-the-system spine** (🟢 address-zoomable public viewer · 🟡 END-mandated-but-geoportal/WMS-only · 🟠 airport/rail-by-operator · 🔴 no public map), verdict bands keyed to the **WHO 2018 Environmental Noise Guidelines** thresholds (road <53/<45 · rail <54/<44 · aircraft <45/<40 · wind <45 Lden), a wind-turbine **statute** table (DK BEK 135/2019 39/44 dB · DE TA Lärm 35 dB night · UK ETSU-R-97 · NL Bal §4.30a 47/41 · FR émergence · IE/SE/FI/PL · US-WI/OR · AU-SA/VIC — dB *limits* citable as fact, per-address exposure flagged as modelled), the named public viewers for the strong core (US BTS · FR-IdF Bruitparif · DE UBA/EBA · NL Atlas Leefomgeving · IE EPA · AT · ES · BE-Brussels · CH [Swiss Lr, kept metric-separate from EU Lden] · UK Extrium [**aircraft EXCLUDED** — load-bearing flag] · DK · EE · CZ · SI · HU · HR), and an honest OSM-proximity + WHO tail for the ~100-country remainder. **(2) notary-deed-anatomy** → `shared/notary-deed-anatomy.md` (~104 lines, a `--notary` sub-extension like `notary-forced-heirship.md`): the clause-level forensic checklist of the **sale-deed text itself**, gated by a prominent **anti-advice boundary rule** (existence-checks — "confirm clause X is present / says Y" — *never* "change clause to Z", which is counsel's job); a strong-anchor table read from primary statute / prescribed forms (UK **TR1** 12 panels · ES **Ley Hipotecaria art. 9** · FR **Décret 71-941** + CC 1583/1589 · DE **§§311b/925/1191 BGB** · AT **§433 ABGB** + GBG · CH **ZGB 657** · IT **art. 2659/2657** · NL **BW 3:89** · PT Casa Pronta · Scotland disposition · NSW **01T** · NZ A&I · BC **Form A**) + an 11-item universal red-flag existence checklist; the ~100+ no-form jurisdictions (incl. BE + unverified GR/MT/CY-civil) honestly left as a structural contrast + "verify with local notary/registry", never slot-filled. **Rejected: (3) `--inflation-adjusted-history`** (REJECT-OVERLAP, HIGH) — every named transaction dataset (UK PPD, FR DVF, JP Reinfolib, NL Kadaster…) is already registered with richer metadata in `comparable-transactions.md` and the CPI deflators are in `macro.md`; the only net-new idea (deflate nominal → real) is a transparent **computation**, so it was folded in as a ~22-line **"Real-terms (inflation-adjusted) historical reconstruction"** method box in `comparable-transactions.md` (deep-history Tier-A set UK 1995 / DK 1992 / SI 2007 / IS 2006 / EE 2003 / IE 2010 / FR 2014 + the BIS real-terms cross-ref) rather than a 121-cell section duplicating that registry. **(4) `--news-digest`** (REJECT-OVERLAP, HIGH) — `regulatory-watch.md` (per-country, date-stamped, primary-sourced) + the `updater.md` render-time regwatch banners + `--health-report` already serve recent-reform surfacing; wiring the raw `feed-watcher.yml` RSS output to a user-facing section would put un-triaged Forum/RSS-tier signal next to a verdict (anti-hallucination breach), so no section/flag was added. Sub-section extensions **11 → 13** (README tree + `docs/usage.md` + `CLAUDE.md` file-map); SKILL.md `--traffic`/`--notary` rows enriched with the extension references; **no new top-level flag → no argument-hint drift, section count unchanged at 41**. Anti-hallucination posture throughout: strategic noise maps reported at **map-band granularity** (never a parcel-precise dB), wind dB *limits* cited as statute fact while per-address exposure is flagged modelled, mirror-only citations (IT brocardi.it, CH swissrights.ch) down-tiered SECONDARY with a primary re-verification path, and a final independent fabrication-audit pass over both files + the fold-in.

- **`--auction-registry` — 41st user-invocable section (plugin 2026.05.66 → 2026.05.67).** Where distressed property is actually published per country — judicial / foreclosure / mortgagee / tax-seizure auctions — with a **classify-the-system spine** (the proven `--title-monitoring` pattern) so the answer stays honest where no portal exists: 🟢 PRIMARY-OFFICIAL central portal · 🟡 regulated-entity / split / gazette · 🟠 fragmented (per-county/court) or aggregator-only (commercial houses) · 🔴 none-confirmed. Canonical `shared/auction-registry.md` (249 lines): universal contract + system spine + verdict bands + 7 traps + 7 mitigations + per-country one-liners across all **121 countries** in 6 regional blocks (+ a Crown-Dependencies pass), each tagged with the named official portal/URL where one exists or an honest "verify with the competent court / official gazette" fallback. The **3-axis foreigner gate** (mechanism access / legal eligibility / underlying land-ownership limit) is a first-class trap — winning a bid is worthless if a foreigner cannot own the land (TH/ID/VN/PH/CN condo-quota/leasehold; OECS/BVI/BM Landholding-Licence; CH Lex Koller). Built **feasibility-gated** (26-market probe → BUILDABLE-with-a-spine verdict) then via 7 region research agents + a Crown-Deps pass → an **independent 4-agent liveness+correctness audit** that GET-verified the official portal URLs and caught fabrication-adjacent errors before shipping: IT is **PVP** not "TPC"; MX "REM@JU" is **Peru's** portal not Mexico's (MX has no single federal portal); FR encheres-publiques is **commercial** not official (no central FR portal — mandatory avocat); NL **veilingnotaris.nl** official vs veilingbiljet.nl commercial; HR **edrazba.fina.hr** (not drazbe.fina.hr); UZ enforcement on **e-auksion.uz**; **EG `auctions.realestate.gov.eg` is a state MLS, not a foreclosure registry**; SA **Najiz** not "Enfaz"; KZ **aisoip.adilet.gov.kz**; QA Mzadat app-only; IN **ibapi.in/baanknet.com** official vs bankeauctions.com private; KR `.go.kr`-vs-`.co.kr` and JP `bit.sikkou.go.jp`-vs-`981.jp` lookalikes flagged. Anti-hallucination throughout — commercial aggregators always tagged SECONDARY (never presented as official); ~31 markets honestly marked 🔴 NONE-CONFIRMED rather than given an invented URL; Jersey *dégrèvement* / Guernsey *saisie* correctly described as creditor-**vesting** procedures (not auctions); Greenland flagged as having no private land ownership. Section count 40 → **41** (full cascade: SKILL.md argument-hint + catalog + `four transaction` grouping; README section list + shared-file table; CONTRIBUTING; labels; PR + ISSUE templates; plugin.json). Distinct from `--type=auction` (how-to-buy-at-auction mechanics) — this is the *where-are-they-published* discovery + registry-classification layer.

- **T2 fold-ins — 4 surgical depth additions across 3 shared sections (the ROADMAP "🟡 Tier-2 fold-ins" bucket; plugin 2026.05.65 → 2026.05.66; NOT new flags — section count stays 40).** Four cross-cutting depth additions, each researched then re-verified by an **independent adversarial audit** before writing (research → audit pipeline; every dated/cited claim primary-sourced or marked UNVERIFIED). **(1) LGBTQ+ & unmarried-partner joint title + co-borrower eligibility (at purchase)** → `shared/finance-banking.md`: joint title is a property-law act decoupled from marital status (common-law joint-tenancy/survivorship vs civil-law fractional co-ownership), lender-side acceptance of unmarried/same-sex co-borrowers (JP **JHF Flat 35** the documented positive case — same-sex co-borrower + income-combine + co-own for applications on/after **4 Jan 2023**, confirmed on both flat35.com and jhf.go.jp), the purchase-time survivor-protection trap (civil-law has no default survivorship/community-property for cohabitants), and the EU cross-border recognition ruling **CJEU C-713/23** *Cupriak-Trojan v Wojewoda Mazowiecki* (Grand Chamber 25 Nov 2025, EUR-Lex CELEX 62023CJ0713; builds on *Coman* C-673/16). **(2) Accessibility & adaptable-dwelling build-code** → `shared/risks-build-quality.md`: AU NCC 2022 **Part H8** Livable Housing (national commencement 1 Oct 2023; **NSW + WA both "Not adopting"**), UK **M4(1)** the in-force baseline with **M4(2) announced 29 Jul 2022 but NOT enacted**, US FHA covers 4+-unit multifamily first-occupied after 13 Mar 1991 (not detached houses; ADA = public accommodations), EU **EAA Dir 2019/882** (28 Jun 2025) covers products/services with the **built environment OPTIONAL** (Art 4(4)) — not a dwelling mandate — plus the UK DFG retrofit grant. **(3) EV-charger build-readiness mandate calendar (2024-2027)** → `shared/risks-build-quality.md`: UK Part S (15 Jun 2022, England only), California CALGreen Title 24 Pt 11 (2025 edition eff 1 Jan 2026), and EU EPBD recast Dir (EU) 2024/1275 **Article 14** (transposition 29 May 2026) — honest tail that most of the corpus has no residential EV-ready mandate. **(4) Closing-day funds settlement: wire cut-offs, cleared-vs-available, FX timing** → `shared/remote.md` (neighbour of the *Hawarden v ENS* BEC trap, cross-linked `shared/currency.md`): Reg CC available≠cleared, verified rail cut-offs (Fedwire customer **18:45 ET**, CHAPS customer **17:40**, SEPA SCT D+1 / SCT Inst 24/7, T2 live 20 Mar 2023), the cross-border time-zone-collision trap, and 3 buyer mitigations (pre-position T-2, confirm the bank's customer cut-off, never FX on completion morning) — PSD3/PSR flagged NOT-yet-in-force. **Anti-hallucination corrections the pipeline caught in the ROADMAP's own anchor claims**: EPBD is **Art. 14 not "Art. 12"**; Fedwire customer cut-off is **18:45 ET not "5pm"**; UK M4(2) is **announced not enacted**; the "75 non-recognising countries" figure was **unverifiable** (dropped, replaced with the verified 38-marriage-equality count). One audit-flagged "correction" (claiming WA adopted Livable Housing) was itself **wrong** and reverted after a direct primary-source check of the ABCB adoption table (WA *is* "Not adopting"). 3 files; 0 forbidden phrasings; no new SKILL.md flag, no argument-hint drift; fold-in-level `Last verified: 2026-06-03` stamps (file-level footers deliberately not blanket-re-stamped). Closes the ROADMAP Tier-2 fold-in bucket.

- **fix(source-verify): JS-SPA / thin-body pages no longer false-flag as `TOKENS_ABSENT` (chips #232; no version bump).** The no-LLM audit asserted `TOKENS_ABSENT` (claimed figure not on the cited page) for any OK fetch whose figure-needle didn't match — including client-side-rendered gov pages (e.g. **Hong Kong e-Legislation `elegislation.gov.hk`**) the fetcher receives as an empty JS shell. A *correct* statute citation (Cap. 7 / 115 / 159 / 344 / 349 / 405 / 621 …) was thus mis-flagged as a missing figure — the bulk of #232's 228-count noise. `build_prior` now downgrades a zero-match OK fetch to **`SOURCE_UNAVAILABLE` (indeterminate)** when the host is a known JS-SPA (`fetch.js_spa_hosts`, seeded `elegislation.gov.hk`) or the cleaned body is below `fetch.min_text_chars` (200) — conservative on the safe side (a tiny page erring to *indeterminate* beats a false *absent*), and it never suppresses a genuine miss (a full page lacking the figure stays `TOKENS_ABSENT`). **Verified end-to-end**: the HK audit's figure-absent count fell **11 → 2** (the 9 e-Legislation false-positives reclassified; the 2 remaining are genuine triage candidates). `config/_source-verify.json` gains `min_text_chars` + `js_spa_hosts`; `scripts/source-verify-test.py` → **95 tests** (4 new — thin-body-indeterminate, thin-body-with-match-still-present, js-spa-host backstop, long-body-no-match-still-absent).

- **regwatch: GE overdue-revisit re-verification (closes #196; no version bump).** The 2 overdue regulatory-watch entries (both Georgia visa, 62 days past `revisit_by`) were re-verified against primary/authoritative sources and re-stamped (`verified_date` 2026-05-27 → 2026-06-03, `revisit_by` 2026-04-01 → 2026-09-01): the real-estate residence-permit threshold (US$100k → **US$150k from 1 Mar 2026**; US$300k/5-yr unchanged; pre-1-Mar purchases keep the legacy threshold) confirmed ([legal.ge](https://legal.ge/en/news/georgia-raises-real-estate-investment-bar-for-residence-permits-by-50-en) · IMI Daily · Savory & Partners); the labour-migration work-permit reform (mandatory from 1 Mar 2026, adopted 26 Jun 2025) confirmed + enriched with the **1 Jan 2027 authorisation/residence deadline** + GEL-2,000/person fine + Int'l-Company/GEL-15k labour-market-test waiver ([Fragomen](https://www.fragomen.com/insights/new-work-permit-rules-georgia-immigration.html) · KPMG · US Embassy Tbilisi). The `ge` playbook already reflected both thresholds — no patch needed.

- **chore(maintain): stale-count fixes (no version bump).** `/maintain` sweep — corrected the `SKILL.md` skill description (`thirty-three sections` → **forty**, adding the seven ownership-diligence sections — property-management / renovation / surveyor / inherited-noncompliance / squatter / latent-defect / title-monitoring — + the reverse-compare `--match` discovery mode) and the README Tier-A token-cost row (`15 countries` → **16**, matching `config/_tiers.json`). GitHub About blurb verified current (121 × 40); doc-sync, JSON/YAML, match-index + visa-programs render checks all green; secret/PII scan clean. Resolved stale issue [#218] (internal link-check — green since the #219 doubled-path fix).

- **`--match` — reverse-`--compare` discovery mode (Phase 1; no version bump, no new section — count stays 40).** Give *constraints* (cohort + budget + criteria) and get a *ranked shortlist of best-fit countries* back, instead of naming the countries. Implemented as a reverse **mode** of `--compare` (logic in `shared/compare.md` § Reverse mode), classified as a discovery mode in **both** `output_only` sets (`scripts/sync-docs.py` + `.github/workflows/pr-validate.yml`) so the section count stays **40**. Reuses the existing precomputed verdict shortcuts + `config/_visa-programs.json` + per-playbook values — never invents a score: hard-filter (foreign-buyer access / budget feasibility / `--must=` visa/FX/residency) → rank-based per-criterion bands (`100 × (K − r + 1) / K`) → cohort-weighted composite, with criteria lacking a grounded value dropped from **both** sides of the average (missing data neither helps nor penalises). 6 cohorts with shown weight vectors; 3 anti-hallucination additions (no fabricated composites · structural-shortlist-not-advice · always print the weighting). `argument-hint` + the `--compare` catalog row + the universal-section-concepts map + README / docs examples updated. **Phase 2** shipped alongside (next entry).

- **`--match` Phase 2 — `config/_match-index.json` deterministic precompute + audit (no version bump).** `scripts/render-match-index.py` generates the index **deterministically** from `shared/compare.md`'s ranked lists + `config/_visa-programs.json`: per-country rank/band + `src` provenance for the 6 already-ranked criteria (acquisition-cost / annual-carry / fx / foreign-buyer / visa / tax) + 3 hard-filter booleans (foreign-buyer-open / no-fx-risk / property-residency-route), covering the **77** countries those lists rank (the rest fall back to live extraction). The 7 judgement-heavy criteria (yield / climate / healthcare / schools / safety / connectivity / cost-of-living) are emitted as `pending-extraction` — **never fabricated** (a separate grounded research + independent-audit pass promotes them; they run live MEDIUM until then). New `scripts/render-match-index-test.py` (**18 unit tests**, all green — banding math, section extraction, Eurozone expansion, ban-keyword friction flags, pending-never-fabricated, provenance, idempotency) + `.github/workflows/match-index-audit.yml` (`--check` keeps the index in sync with compare.md + runs the tests; mirrors `visa-programs-audit.yml`). Eurozone members pinned to the top FX tier (the "no FX risk" group isn't spread by list-expansion order). `shared/compare.md` § Reverse mode wired to consume the index. Full design: `_local/scope-match-section.md`.

- **`--match` Phase 2b — healthcare tiering + feasibility finding (no version bump).** A feasibility probe (per the feasibility-first gate) found **6 of the 7 pending criteria are not groundable as a static index**: safety / connectivity / yield / climate / cost-of-living / schools have **no stored per-country value** in the repo — the skill computes them live/per-query from primary sources *by design* (so they never go stale), so pre-baking them would decay or fabricate. They correctly stay `pending-extraction` → live MEDIUM extraction (the FR/PT playbooks carry no crime rate or FTTH %; LC's `--crime` explicitly says *"do NOT cite a homicide rate without a primary check"*). The **one exception is healthcare**: `shared/digital-nomad-healthcare.md` carries a stored categorical signal (public-system enrollment timeline), curated into new `config/_match-extracted.json` for **10 countries** (DE/CH/JP immediate · NL/FR/CA fast · KR/TW/IL 6-month · ES 1-year contribution), each with source + verbatim evidence + tier, merged into the index at MEDIUM confidence. `scripts/render-match-index.py` extended to load + merge the curated file (kept regenerable; CI `--check` covers it); `scripts/render-match-index-test.py` updated (the six live-only criteria asserted **always-pending**, healthcare cells asserted grounded). The feasibility gate prevented an 847-cell fabrication.

- **`--match` independent audit + hardening (no version bump).** Ran 3 adversarial auditors before shipping (guardrails · data-provenance · engineering-correctness). **Provenance audit: 0 findings** — every band traces to a real source, all 462 live-only cells `pending` (zero fabricated values), all 10 healthcare evidence phrases verbatim in source. **Fixed 2 HIGH + 1 MEDIUM correctness bugs** in `scripts/render-match-index.py`: the golden-visa parser banded 5 "NO golden-visa" countries (SG/IN/NG/KE/KW) as visa-friendly → now skipped via an absence filter; `property-residency-route` used exact `status == 'OPEN'` → now `startswith('OPEN')` so qualified-OPEN routes (GR/MT/EG) count; the ban-keyword heuristic was inconsistent (JO/OM/BH closed vs identical AE/QA/SA open; VN land-ban missed) → refined so GCC designated-zone countries are uniformly open and VN/TH/ID/PH land bans are caught. **Fixed 2 HIGH + 2 MEDIUM guardrail gaps** in `shared/compare.md` § Reverse mode: added a **No-data rule** (descriptive prose is NOT a band source; no discrete value → render `—` and drop from the composite — closes the live-extraction-fabrication hole the runtime LLM faced on the 6 zero-stored-value criteria), fixed the worked example that *modelled* the fabrication (PT shown `Climate 🟢` despite climate being `pending` → now `—`), defined "grounded" inline, relabelled the 6 zero-data criteria **LOW / data-not-available** (not uniform MEDIUM), and added a "<50% grounded weight → `LOW — partial` composite" guard. Negative tests added for every correctness fix (**24 tests**, all green). MC/AD pinned to the no-FX-risk top tier.

- **Source-content verifier — free deterministic (no-LLM) audit package + monthly cron (no version bump; CI tooling).** Makes the cheap half of the verifier schedulable at **$0** (public-repo runner, ~3–4 min cold / ~2 min warm over the 706 cited URLs). Four pieces: **(1) PDF text extraction** — `source-verify.py` now extracts cited PDFs via poppler `pdftotext -layout` (preserves fee/rate-schedule table columns) with a `pypdf` pure-python fallback, so the ~45 PDF primary sources get token-checked instead of skipped as binary; a scanned-image PDF lands in a new `PDF_NO_TEXT` status, never a false `TOKENS_ABSENT`. **(2) Content-change tripwire** — hash-diffs each cited page across runs; the `stale_changed` bucket (page changed *after* the claim's `Last verified` date) is the freshness signal `url-liveness` structurally cannot give. **(3) `--audit` mode** — runs the whole pipeline with no model, emitting a deterministic `TOKENS_ABSENT` + `CONTENT_CHANGED` report (`--ci` writes `_ci/source-verify/` + `GITHUB_OUTPUT`). **(4) `scripts/check-citations.py`** — pure-static portal-root linter (no network, no deps) flagging bare-host citations next to a percentage/money figure (the `[gov.ky](gov.ky/)`-next-to-CI$2M pattern), tuned to skip the accepted `[Llei 21/2014](gazette-root)` statute-naming convention + `verify/confirm-at` pointers → 154 advisory findings (down from 616 unfiltered), surfaced as a backlog in the audit issue rather than a per-PR blocker (the standing count would be noise). New `.github/workflows/source-verify.yml` (monthly cron `23 9 1 * *` + dispatch; mirrors `url-liveness.yml` — pinned `ubuntu-24.04`, content-cache artifact for the change-detection baseline, rolling `source-verify` issue, **a PDF-extractor smoke test that fails run #1 loudly if poppler/pypdf are both unusable**). **Two bugs the build's own smoke test caught**: PDFs were truncated to an invalid file by the 600 KB HTML cap (PDFs must be read in full — added a 12 MB pdf cap), and `resp.content.read(n)` returns only the first ~32 KB chunk (not `n` bytes) — so long HTML pages were being token-checked in just their first 32 KB, a latent false-`TOKENS_ABSENT` source; fixed with a proper chunked read (`TOKENS_PRESENT` rose 14→22 on the `hk` smoke scope, content-change false-positives → 0). `config/_source-verify.json` gains `pdf` + `linter` blocks; `scripts/source-verify-test.py` → **85 unit tests** (PDF extraction incl. truncated/empty/max-pages edge cases, `_is_pdf` content-type-vs-path-suffix + query-string handling, content-change matrix, `_changed_after_stamp` malformed-date graceful paths, audit-report buckets + `flagged`-distinct, `render_audit_md` pipe-escaping, linter `flag_line` precision across statute/pointer/allowlist/root/document-label cases). Small refactors for testability (`_is_pdf` / `_detect_change` / `flag_line` pure helpers) surfaced + fixed a `flagged` double-count (a claim both figure-absent AND page-changed counted twice). The audit **never** judges correctness or edits anything — it only surfaces which citations to look at; the LLM adjudication stays on-demand. Per-PR `pr-validate.yml` deliberately **not** wired (advisory-with-154-standing-findings would be ignored noise). **Follow-up (validated live):** the first `workflow_dispatch` run (#231 merged → run on `main`) passed all steps and filed `source-verify` issue #232 (224 figure-absent baseline · 0 page-changed on cold start). It surfaced `pypdf`'s broken-PDF recovery chatter (it's installed in CI, absent locally) — silenced the `pypdf` logger, added a `%PDF-` magic-byte guard so a `.pdf` URL that serves an HTML interstitial is token-checked as HTML (not force-failed to `PDF_NO_TEXT`) and the extractors are never fed non-PDF bytes. **Second live run** (with the cache restored → content-change active) exposed both noisy signals and drove the hardening that makes them trustworthy: **(a) linter scoped to PRIMARY-SOURCE roots** — 119 of 154 findings were commercial carrier/aggregator homepages cited for tariffs (`orange.sk`, `telekom.hu`, `flat35.com`), an accepted convention, not the weak-citation pattern; `check-citations.py` now flags only gov-suffix/allowlist roots → **35** genuinely-actionable findings (a deep gov page exists and should be cited). **(b) content-change settle-confirmation** — a naive "hash differs from last run" flagged 41 pages (~6%), all dynamic gov portals (central-bank rate tickers, stats homepages, news pages — `bce.fin.ec`, `stat.gov.kz`, `moi.gov.tw/News`), zero real figure changes; `_settle_change` now only confirms a change once the page has **settled** at a new hash across two consecutive runs (dynamic pages never settle → filtered; a genuine change surfaces one cycle later), storing a `stable_hash` baseline. **91 tests.**

- **Source-content verifier — first production run: 14 citations re-pointed + 2 factual corrections across 14 playbooks (no version bump; grounding-only).** First real use of the harness (below). The 40-claim corpus pass surfaced 17 `NOT_FOUND` weak citations; **17 research agents** found the page/PDF/secondary that actually states each figure (hard rule: return `NO_CLEAN_SOURCE` rather than invent a URL), and an **independent 15-agent confirmation pass** re-fetched every re-pointed URL (4 `MATCH` / 9 `PARTIAL` — each URL substantiates the specific fact it anchors while other figures on the same line cite elsewhere / 2 `NOT_FOUND` that drove two further fixes). **14 citations re-pointed** from portal-roots / migrated-stubs to the substantiating primary source, each verbatim-confirmed: HK IRD `brfee_table.pdf` (BR fee 2,200/5,720 row), KY Stamp Duty Law 1973 PDF (1% ≤CI$300k / 1.5% > mortgage duty), PT DRE `Decreto-Lei n.º 126/2021`, HU decree `389/2024. (XII. 11.)`, TW Land Act Art. 17 forbidden-land list, VG `Non-Belongers Land Holding Regulation Act (CAP.122)`, GE NBG larization-measures (FX-loan restriction <GEL 200k since 2019), MO `Lei 7/2023` Boletim Oficial, SG BCA works-requiring-approval page (replacing a dead 200-returning migration stub `url-liveness` cannot catch), BM Work-From-Bermuda FAQ (end-date 28 Feb 2025), BM Development & Planning Amendment Act 2018, GD Deeds & Land Registry Act Cap. 79, AZ PwC property-tax (authoritative-secondary — no isolable primary), FR ANIL LF-2026 analysis. **2 factual corrections** the verification surfaced (figures, not just links): **KY** — the *"CI$2,000,000 for the 25-year Residency Certificate"* figure **conflated two products** (CI$2M is the *lifetime* Certificate of Permanent Residence; the 25-year certificate's threshold is different/lower) → corrected + flagged with **no fabricated source** (the agent returned `NO_CLEAN_SOURCE`); **FR** — the RVLLH reform was **misattributed to "PLF 2024/2025"** → corrected to **LFI 2026 (art. 106, LOI n° 2026-103 du 19 février 2026)**, tax-impact year fixed **2030 → 2031** (2028 declarations → 2030 new values → 2031 taxation, per ANIL + Légifrance — the confirmation pass caught this residual error in the first correction), and the unsourced *"+20–40 % rural"* relabelled an unverified estimate. 1 verifier flag was a false positive (IN TRAI — the URL is only cited for a descriptor it does support) and left unchanged. **Grounding-only**: no figure changed without a verbatim primary/authoritative source; genuinely-unverifiable components left flagged, never fabricated. 14 files, 16 lines; no `Last verified` footers blanket-re-stamped (only the touched facts carry inline `2026-06-02` markers where re-verified).

- **Source-content verifier harness — re-reads cited sources to check the claimed figure against the page (no version bump; new tooling, no SKILL.md flag).** Attacks the one gap `url-liveness` structurally cannot: a URL returning 200 proves the source is *alive*, not that the number next to it is what the page states (CLAUDE.md: *URL-liveness ≠ data-still-current; don't re-stamp on a 200 alone*). Two layers. **(1) Deterministic harness** `scripts/source-verify.py` — extracts claim↔**primary-source** pairs (a markdown link to a `.gov`/`.gouv`/`.gob` host or a `primary-source-allowlist.txt` entry whose sentence carries ≥1 *salient* value: percentage / money / statute / specific date — bare years and roster links filtered out), samples them **fetch-free** (`stale-marker` > never-verified > oldest stamp, so the network footprint is bounded by `--sample`), fetches + cleans + caches each cited page **reusing `url-liveness.py`'s polite-fetch primitives** (robots.txt, per-host throttle, gov-suffix floor, EUR-Lex crawl-delay, Cloudflare detection, excluded hosts — politeness stays single-sourced in `config/_url-liveness.json`, never duplicated), then runs a **token-presence pre-filter** emitting a 3-tier prior: `TOKENS_ABSENT` (the claimed figure is **literally not on the cited page** — a deterministic bug-catch, surfaced with a `⚠️` count before any LLM runs), `WEAK_MATCH` (only noise-prone 1–2-digit needles hit, e.g. "4" from "Art. 4 B"), `TOKENS_PRESENT` (a specific ≥3-char figure matched, with a windowed excerpt). **(2) LLM verification layer** spec'd in `shared/source-verifier.md` — a Workflow (or Agent fan-out) reads the worklist and emits per-claim verdicts (`MATCH`/`MISMATCH`/`STALE`/`PARTIAL`/`NOT_FOUND`/`SOURCE_UNAVAILABLE`/`INDETERMINATE`) under hard guardrails: **quote-or-abstain** (no MATCH without a verbatim source span), **never auto-edit / never re-stamp `Last verified`**, reject coincidental number collisions as `INDETERMINATE`, no silent source-tier upgrades. New `config/_source-verify.json` (extraction/verification knobs only) + `scripts/source-verify-test.py` (**37 unit tests, all green** — value extraction, primary-source gating, 3-tier prior, sample selection, thousands-separator + full-month needle normalization, dedupe-by-id, HTML→text, cache TTL). **Validated live with a 40-claim corpus pass** (fan-out of Opus verification agents, quote-or-abstain): **0 MISMATCH / 0 STALE** (no claim contradicted by its own cited source), **10 verbatim `MATCH`**, and — the payoff — **17 `NOT_FOUND` weak citations** (claim points at a portal *root* / migrated page / a figure that lives one hop down in a PDF, not the page that proves the number) including a **dead-content-behind-200** (a Singapore gov FAQ now a "page revamped/moved" stub still returning HTTP 200 — the exact failure `url-liveness` structurally cannot catch), plus 8 `SOURCE_UNAVAILABLE` (bot-block / JS-SPA / TLS / timeout — fetch problems, not claim problems). The 17 `NOT_FOUND` became an actionable citation-re-pointing checklist (9 portal-root / 4 attributed-to-a-secondary-source / 3 dead-content-behind-200 / 1 figure-in-PDF). The verification layer correctly **upgraded coarse `TOKENS_ABSENT` priors to `MATCH`** where the figure was present but formatted differently (Sri Lanka "No. 41 of 1978", a St Kitts PDF, IRS Pub 523 §1250), and rejected coincidental number collisions (a "© 2026" copyright year, hotline digits) as non-matches — proving the two-layer division of labour. Two refinements the pass drove are folded in: thousands-separator-stripped + full-month-name needles (cut `TOKENS_ABSENT` false-positives from format mismatches), and dedupe-by-id — the corpus now reports **971** unique primary-source-cited claims (1000 raw − 29 same-URL-twice duplicates collapsed). Operational lesson baked into the doc's Workflow: run verification in **throttled batches of 8** — at ~16-wide concurrency later agents fail to emit StructuredOutput under load (24/40 failed at 16-wide → 0/24 at 8-wide on this run). On-demand + sampled (not an unattended cron) — point it at a country before re-stamping it, or at the corpus-wide `TOKENS_ABSENT` set. Worklist + content cache live under git-excluded `_local/source-verify/`; verdict reports under `_local/reports/`. **Decision-support only** — it checks the *cited* page, not the world; a `MATCH` moves a claim from internally-consistent toward externally-grounded but does not eliminate the permanent cross-jurisdiction accuracy tension (it shrinks the residual).

- **Router slim + count-drift guards + stale-count sweep (`/maintain` follow-up, no version bump).** A maintenance pass triggered by an external review that flagged (a) the SKILL.md router as too heavy and (b) a `121`-vs-`103` count inconsistency. **(1) Router slim** — the SKILL.md section-catalog had **26 bloated cells** that dumped each section's full statute list + 2024-2026 reform calendar + per-country specifics inline (the `--rental`/`--risks`/`--home-tax`/`--relocation`/`--schools`/etc. rows ran ~1,000 words each). Collapsed each to a lean one-liner = *what it produces* + the `shared/X.md` pointer + the routing-critical *"Distinct from …"* clause; the dropped detail is unchanged and canonical in the referenced `shared/*.md` (verified present token-by-token before removal — each shared file is 500-1,089 lines and carries the detail many times over). ~42k code-points (~11k tokens) off the router with zero content loss; `argument-hint` flags + the hand-maintained country matrix untouched, so the arg-hint↔README and country-matrix CI gates stay green. **(2) Two alert-only count-drift guards** for the two channels `sync-docs.py` cannot see (it only rewrites `**N countries**` bold/AUTOGEN markers): `scripts/check-prose-counts.py` flags stale corpus-**scope** country counts written in free prose (classifies scope-must-equal-corpus vs coverage-may-be-partial; excludes CHANGELOG/ROADMAP history, country-playbook populations, and the SKILL.md matrix rows), and `.github/workflows/check-counts.yml` (monthly + `workflow_dispatch`) compares the live **GitHub About** blurb's country/section numbers against canonical AND runs the prose guard, opening/updating a pinned `count-drift` issue on drift and closing it on resync — it never auto-edits the About (that needs an admin PAT) and never blocks a merge. New `count-drift` label.

- **Batch D cross-cutting backfill — Wave 3 (living/build/succession cluster) — COMPLETES the backfill** (plugin 2026.05.64 → 2026.05.65). Adds the 8 new Caribbean countries (`ky`/`bm`/`tc`/`vg` + `kn`/`ag`/`lc`/`gd`) to the per-country tables of the **final 12 cross-cutting shared sections**: `notary-forced-heirship`, `risks-build-quality`, `renovation`, `relocation`, `digital-nomad-healthcare`, `mains-reliability`, `connectivity`, `language`, `schools`, `crime-sources`, `property-management`, `rental-yield-delta`. With Waves 1 (#215, 7 sections) + 2 (#216, 9 sections), **all 28 cross-cutting shared sections now carry the 8 Batch-D countries** — the legacy-denominator TODO is resolved. Two-wave **research → independent-audit** (12 per-section research agents + 2 audit agents); **both audits 0 fabrications, no corrections required**. Notable verified findings + caught traps: **Saint Lucia has testamentary freedom / NO forced heirship** (its Civil Code is the Quebec-1866 model, not French — the agent refuted the brief's assumption and caught a slot-fill trap where a "legitime" search hit was verbatim Philippine Civil Code text); **Cayman has statutory contractor licensing** (Builders' Board / Builders Regulations 2018 — refuting "reputation-only"); BM "Work From Bermuda" DN certificate **discontinued 28 Feb 2025**; GD/LC DN visas active, KN none; Hague-apostille dates day-exact vs HCCH (BOTs via UK extension 25 Apr 1965; KN 1994 / AG 1981 / LC-GD 2002); Starlink dated per island (KY licensed 23 Dec 2025 OfReg); GD non-resident rental WHT 15% / 7-day-remit / deductor personally liable (IRD primary); BM 🔴 STR-regulatory-kill (Minister's permission to let, conditional on being off-island); BM non-residents cannot own/drive a full-size car; IB World Schools (AG Island Academy, BVI Cedar International) verified; no statutory new-build décennale in any (common-law + escrow). Small-jurisdiction specifics that aren't separately published tagged `verify`/`data not publicly available`, never fabricated. `language` + `connectivity` regional-block headers extended (+ connectivity Starlink-registry rows). **NOT a new flag — section count stays 40.** Applicability-scope statements bumped to "all 121"; per-country one-liner *counts* remain honest. **Known follow-up (out of scope here):** four files (`notary-forced-heirship`, `risks-build-quality`, `digital-nomad-healthcare`, `schools`) still carry an obsolete pre-Crown-Deps meta-sentence ("the 6 region blocks cover all 100 countries … schema-blocked territories default to parent jurisdiction") whose premise is now false (those territories are standalone playbooks) — flagged for a separate doc-correction.

- **Batch D cross-cutting backfill — Wave 2 (finance/tax/transaction cluster)** (plugin 2026.05.63 → 2026.05.64). Adds the 8 new Caribbean countries (`ky`/`bm`/`tc`/`vg` + `kn`/`ag`/`lc`/`gd`) to the per-country tables of **9 more cross-cutting shared sections**: `finance-banking`, `finance-crossborder`, `exit-seller-withholding`, `currency`, `exit` (→ `shared/exit/caribbean.md` + the `exit.md` region table/heatmap), `agent`, `permits`, `scams-postcompletion`, `integrity-remorse`. Two-wave **research → independent-audit** (9 per-section research agents + 2 audit agents); **both audits returned 0 fabrications** (one factual fix: Grenada CBI-project de-certification re-dated to **Aug 2023**). Verified-vs-hedged highlights: FATCA IGA models (KY/VG/TC Model 1, **BM Model 2 outlier**, ECCU-4 Model 1B); currency pegs (KYD 1.20, BMD/TC/VG USD, XCD 2.70); all 8 are **Model C** for non-resident-seller tax (no CGT-clearance gate — GD 15% / LC 10% non-citizen *vendor* transaction taxes correctly distinguished from a gains gate; AG LVAT hedged); crypto frameworks (BM DABA 2018, KY/BVI VASP Acts, **TC crypto unauthorised** per TCI FSC Nov-2025); the offshore-SPV look-through (BVI BC / Cayman exempted co are tax-neutral but local land is still caught by NBLHL / Cayman Land Holding Companies Share Transfer Tax / BM Part-VI / OECS ALHL); Cayman **CIREBA** + Turks & Caicos **TCREA** are regulated MLSs (corrected agent.md's now-false "no MLS in the region" claim); all 8 permit statutes confirmed by name/year. Unpublished small-jurisdiction specifics tagged `verify`/`data not publicly available`, never fabricated. **NOT a new flag — section count stays 40.** Section-applicability scope statements read "across all 121 supported countries"; the per-country one-liner *counts* in the SKILL.md catalog rows were set to the honest **111** (actual coverage) for partial sections — and title-monitoring's Wave-1 row corrected 121 → 111 (it had been over-bumped) — while `--sanctions` legitimately stays 121 (complete). Remaining cross-cutting sections (notary-forced-heirship / risks-build-quality / rental-yield-delta / property-management / renovation / relocation / language / connectivity / mains-reliability / digital-nomad-healthcare / schools / crime-sources) backfill in later waves.

- **Batch D cross-cutting backfill — Wave 1 (registry/title/fraud cluster)** (plugin 2026.05.62 → 2026.05.63). The 8 new Caribbean countries (`ky`/`bm`/`tc`/`vg` UK OTs + `kn`/`ag`/`lc`/`gd` CBI) were missing from the per-country tables in the cross-cutting shared sections — they implement the core sections but weren't backfilled into the shared `--`-flag coverage. This wave adds all 8 to **7 shared sections**: `sanctions.md`, `title-monitoring.md`, `scams.md`, `inherited-noncompliance.md`, `latent-defect.md`, `squatter.md`, `surveyor.md` (so e.g. `--sanctions` for Cayman now has a per-country bullet). Built on a two-wave **research → independent-audit** pipeline (7 per-section research agents + 2 audit agents); **both audits returned 0 fabrications** across all 8×7 = 56 entries. Highlights of what was verified vs honestly hedged: Cayman BOT Act (1 Jan 2025) + legitimate-interest regs (28 Feb 2025) + FRA/CAYFIN; BVI legitimate-interest BO access from 1 Apr 2026 (FSC Circular 11/2026); Bermuda BO Amendment Act 2025 (in force 30 Dec 2025); adverse-possession periods (KY/TC/VG/KN/AG/GD 12 yr · **BM 20 yr** Limitation Act 1984 s.16 · **LC 30 yr** Civil Code art. 2103A); **Saint Lucia correctly treated as civil-law** for hidden defects (redhibitory warranty, Civil Code arts. 1417/1432/1434/1436/1440 — NOT caveat emptor, unlike the 7 common-law peers); per-island planning authorities + completion-cert regimes; RICS/BCQS surveyor presence + OECS Building Code 2016; Grenada = deeds-recording (not Torrens) → title-insurance backstop. Offshore BO-register access tiers + small-jurisdiction specifics that aren't separately published are tagged `verify`/`data not publicly available`, never fabricated. Coverage denominators bumped to 121 in the 7 files + their SKILL.md catalog rows. **NOT a new flag — section count stays 40.** Remaining cross-cutting sections (finance/notary/relocation/healthcare/etc.) backfill in later waves.

- **`--climate` forward + near-property-resolution projection layer** (extension to `shared/climate-projections.md`; plugin 2026.05.61 → 2026.05.62). The salvageable slice of the rejected `--climate-future` idea (which a feasibility + independent-audit pass found NOT-buildable as a top-level section because it overlaps the existing `--climate`). The new layer names the **only sources that give a FORWARD projection at near-property resolution**: a government core — 🇬🇧 England **NaFRA2 "Check long-term flood risk"** (`check-long-term-flood-risk.service.gov.uk`, **postcode/area-level, not individual property** per the service's own caveat; 2050s, UKCP18+SLR) + 🇳🇱 **Klimaateffectatlas** (`klimaateffectatlas.nl`, ~100 m grid, 2050/2100 KNMI'23) — plus a clearly model-tier NGO layer (First Street/RiskFactor US parcel · Climate Central coastal SLR · Coastal Risk Australia) and the honest tail (the ~115 other jurisdictions have **no** forward address-level government portal → fall back to the section's existing IPCC AR6 regional + Copernicus ~25 km layer, NOT address-level). Includes a **trap**: current-hazard portals that look authoritative — 🇫🇷 Géorisques · 🇺🇸 FEMA NFHL · 🇮🇹 IdroGEO · 🇯🇵 MLIT/GSI · 🇦🇺 ARR (an engineering design guideline) — are **NOT** forward projections and belong under `--risks`. **NOT a new flag** — section count stays 40; this is a sub-section extension of `--climate`. Facts carried from this session's `--climate-future` feasibility + audit pass.

- **Batch D Phase B — 8 new Caribbean country playbooks (country count 113 → 121)** (plugin 2026.05.60 → 2026.05.61). Standalone playbooks for the 4 **UK Overseas Territories** — `ky` Cayman Islands · `bm` Bermuda · `tc` Turks & Caicos · `vg` British Virgin Islands (new `config/_regions.json` region `caribbean_ot`) — and the 4 **CBI states** — `kn` St Kitts & Nevis · `ag` Antigua & Barbuda · `lc` Saint Lucia · `gd` Grenada (extend `caribbean`). All Tier-C (`config/_tiers.json`). Each ~620-720 lines, mirroring the `bb` Barbados structure (country profile + foreign-buyer-eligibility + 13 `--` sections + cost benchmarks + caveats + verification authorities + Status). Built on the two-wave **research → independent-audit** pipeline (8 jurisdiction research agents + 2 audit agents + 8 authoring agents); audit corrections applied verbatim: **Saint Lucia's final appellate court is the CCJ (since 20 Jul 2023), not the UK Privy Council**; Cayman registry statute = "Registered Land Law (2018 Revision)"; verified post-2024 OECS CBI real-estate floors (KN US$325k condo / US$600k home · AG US$300k · LC US$300k — CIU FAQ still shows a stale US$200k · GD US$270k); Grenada's US E-2 treaty access is gated by the AMIGOS Act (Dec 2022) 3-year-residence requirement; Bermuda ARV foreign-buyer floors (house ≥ $126k / condo ≥ $25,800) PRIMARY with licence fees tagged secondary; BVI Non-Belongers Land Holding Licence + 12% non-Belonger duty under CAP.122; Barbuda communal/leasehold tenure flagged. Anti-hallucination throughout — every numeric fact dated + sourced, listing-derived figures tagged "per listing — verify", genuine gaps marked "data not publicly available — verify at <authority>"; 0 forbidden phrasings. Country-count cascade 113 → 121 across README (matrix auto-rendered from `_regions.json` + counts), `plugin.json`, `SKILL.md`, `CONTRIBUTING.md`, ISSUE_TEMPLATE, `regulatory-watch.md`, `updater.md`. Shared-section per-country coverage denominators (e.g. "applicable across all 113 countries" in `scams.md`/`sanctions.md`/`title-monitoring.md`/etc.) deliberately left at 113 — the 8 new playbooks implement the core sections but are not yet backfilled into the cross-cutting tables, so bumping would overclaim (legacy-denominator rule). **Phase C (5 medium-signal NA/BW/VU/TT/PK → 126) remains queued — not greenlit.**
  - **Follow-up fix**: PR #212 squash-merged on its first commit before the matrix-row commit landed (the "Country matrix ↔ countries/* audit" is non-required), so `main` briefly held 121 country dirs but only 113 SKILL.md matrix rows. Restored to 121 matrix rows (added `ky`/`bm`/`tc`/`vg`/`kn`/`ag`/`lc`/`gd` rows + "all 121 supported countries"), re-establishing matrix↔filesystem parity. No version change.

- **`--sanctions` — 40th user-invocable section** (plugin 2026.05.59 → 2026.05.60). Counterparty sanctions-screening at deed signing: screen the SELLER, the ultimate beneficial owner (UBO) behind any company / SPV / trust seller, and any PEP connection against the four free public lists — **US OFAC** (SDN + Consolidated, `sanctionssearch.ofac.treas.gov`), **EU** Consolidated Financial Sanctions List (FSF), **UK FCDO UK Sanctions List** (the legacy OFSI Consolidated List closed 28 Jan 2026), **UN** SC Consolidated List — so a foreign buyer does not fund a blocked person and find the funds frozen / the deal void. Canonical `shared/sanctions.md` (219 lines): the four-list spine + the **OFAC 50% Rule** + the **FATF gatekeeper model** (Rec 22 DNFBP CDD · Rec 12 PEPs · Rec 24/25 BO transparency) + a **BO-register access-tier taxonomy** (PUBLIC / legitimate-interest / authorities-only / none), 7 cross-cutting traps, 7 mitigations, and per-country bullets (BO register + access tier · AML supervisor / FIU · gatekeeper duty · realistic screening path) across all **113 countries** in 6 regional blocks. Built on the two-wave research → independent-audit pipeline (6 region content agents + 3 independent audit agents); the audit contradicted **0 of the load-bearing claims** and folded in 5 precision fixes (UAE Cabinet Decision 109/2023 issued 6 Nov 2023; IT D.Lgs. 210 of 31 Dec 2025; GI register free since Feb 2026; Serbia BO Act in force 14 Mar 2025; Bosnia not added at the 13 Feb 2026 FATF plenary). **Honesty frame enforced throughout**: a BO register existing ≠ a buyer can search it — since the CJEU's 22 Nov 2022 ruling (C-37/20 & C-601/20) most EU registers are legitimate-interest or authorities-only, so the realistic screener is the regulated notary / lawyer / agent, never buyer self-serve. Section count 39 → 40 (SKILL.md argument-hint + catalog; full count cascade across README / CONTRIBUTING / ROADMAP / docs/usage / labels / PR-template / ISSUE_TEMPLATE / plugin.json / regulatory-watch / updater; argument-hint↔README gate satisfied). Distinct from `--scams` (fraud taxonomy), `--notary` (deed process), `--finance`/`--cross-border` (buyer's own bank KYC), `--title-monitoring` (post-ownership title fraud).

- **Batch D Phase A — schema decision committed (CONTRIBUTING.md + ROADMAP.md; mirror of Batch C PR #185)**. Decides how the 13 queued Batch D jurisdictions slot into the schema before any playbook is written. Validated by a research pass **and an independent fabrication-audit pass** (per the anti-hallucination contract): all 4 **UK Overseas Territories** (`ky` Cayman / `bm` Bermuda / `tc` Turks & Caicos / `vg` BVI) verified STANDALONE — each has its own fiscal authority + land registry + court system, confirmed against primary gov sources (gov.ky/judicial.ky, gov.bm, judicial.tc/gov.tc, bvi.gov.vg/eccourts.org). The **Eastern Caribbean Supreme Court** shared-court question is resolved: a shared appellate layer does NOT break the "own court system" test (each member has a resident High Court division; consistent with the sovereign ECSC states LC/GD/AG/KN already treated standalone). **VG's court is correctly named "High Court of Justice (Virgin Islands)", a division of the ECSC — never a fabricated "BVI Supreme Court"** (the known fabrication trap; audited and avoided). CBI 4 (`kn`/`ag`/`lc`/`gd`) + medium-signal 5 (`na`/`bw`/`vu`/`tt`/`pk`) confirmed sovereign-standalone (no schema ambiguity). **Region placement**: new `caribbean_ot` for the 4 UK OTs (mirrors the `nl_caribbean` precedent; accommodates BM's North-Atlantic location); CBI 4 + `tt` extend `caribbean`; `na`/`bw` → `africa`, `vu` → `apac`, `pk` → `south_asia`. **Audit-caught Phase-B content traps carried forward**: TT's apex court is the UK Privy Council (JCPC), NOT the CCJ (T&T joined only the CCJ *Original* Jurisdiction); PK land records are PROVINCIAL not federal (per-province caveat like AU); VU is leasehold-only (1980 Constitution abolished freehold); KY land statute is "Registration (Land) Law 1996 Revision". Country count unchanged at 113 (Phase B/C ship the playbooks). **Phase B (8 Caribbean, 113→121) + Phase C (5 medium, 121→126) NOT greenlit — queued for project owner approval.** No version bump (schema/documentation only). Research + audit memos at `_local/wip-batchd/` + `_local/wip-audit/audit-batchd.md` (gitignored).

- **4 regulatory-watch entries shipped (the ROADMAP "🔵 Regulatory-watch additions" bucket — no greenlight needed)**, each verified against primary/authoritative sources before writing. Two anti-hallucination catches in the source ROADMAP notes: (1) **Fedwire ISO 20022** — ROADMAP said "1 Mar 2025"; actual is **completed 14 Jul 2025** (rescheduled from the originally-announced 10 Mar 2025 per the Fed's 13 Feb 2025 announcement — [frbservices.org](https://www.frbservices.org/resources/financial-services/wires/iso-20022-implementation-center)); entry corrected. (2) **Zillow/First Street climate data** — ROADMAP said "removal (Nov 30 2025)"; actual is a **mid-Nov 2025 de-emphasis** (data still linked, far less visible) driven by a California Regional MLS accuracy dispute, and Redfin + Realtor.com **declined to follow** — phrased accurately, no clean "removal" claim ([CNN 2 Dec 2025](https://www.cnn.com/2025/12/02/climate/zillow-climate-data-extreme-weather-first-street-redfin), cross-checked TechCrunch + HousingWire); unverified "Casten letter" detail dropped. (3) **NL Box-3 2028** — added the missing `2028-01-01 (pending)` Wet werkelijk rendement entry (distinct from the existing `2026-01-01` transitional one); verification surfaced that the bill passed only the **Tweede Kamer** (12 Feb 2026) and faces **Eerste Kamer rejection risk** + Minister-flagged amendments, so the 2028 date is **not certain** — caveat added. (4) **California NHD** — `foundational` entry for the Natural Hazard Disclosure Statement ([Civ. Code §§ 1103–1103.15](https://leginfo.legislature.ca.gov/faces/codes_displaySection.xhtml?lawCode=CIV&sectionNum=1103.2.), form at § 1103.2; 6 mapped zones: 2 seismic / 2 fire / 2 flood); filed under **🇺🇸 US**, not 🇨🇦 CA (Canada) — "CA" here is California the state. **Playbook patches**: `countries/us/playbook.md` § `--risks` gains an explicit California NHD callout + a "don't rely on listing-portal climate badges" note (pull FEMA + RiskFactor.com + state NHD directly); `countries/nl/playbook.md` § `--tax` Box-3 line softened from a flat "effective 1 Jan 2028" to "intended … Eerste Kamer passage NOT yet secured". New facts carry inline `2026-05-28` stamps; the US playbook's global `Last verified: 2026-05-01` footer is intentionally **not** bumped (only the added items were re-verified, not the whole playbook). No version bump (Tier 2–4 regwatch additions — CalVer micro-bump reserved for Tier-1 ENDED-programme supersession).

- **ROADMAP — Feature candidates queued (16 items: sections + extensions + fold-ins + regwatch)**. Compiled from Reddit depth-survey #6.5 (5 angles · 85 threads · 25 subreddits + primary-source verification — `_local/reddit-depth-survey-6.5.md` gitignored) plus maintainer brainstorm of novel features. **3 novel sections** (`--sanctions` OFAC/EU/UK/UN/PEP screening at deed signing · `--climate-future` IPCC AR6 30-yr address-level projection · `--match` reverse-compare cohort→ranked country list); **1 borderline-buildable extension** (transport-noise: flight + rail + wind-turbine, anchored EU END Directive 2002/49/EC + US DOT NTNM + UK ETSU-R-97 + IEC 61400-11 + WHO 2018); **4 medium-novelty additions** (`--auction-registry` off-market/distressed + `--deed-anatomy` per-country deed forensic + `--inflation-adjusted-history` real-terms price reconstruction + `--news-digest` wraps feed-watcher); **4 surgical fold-ins** (LGBTQ+ joint title + pair-loan into `finance-banking.md`; accessibility build-code into `risks-build-quality.md`; wire-cutoff + FX-on-signing into `remote.md`+`currency.md`; EV-charger mandate into `risks-build-quality.md`); **4 regulatory-watch entries** (Zillow climate-risk-data removal Nov 2025 · Fedwire ISO 20022 migration · NL Box-3 2028 · CA NHD as US-playbook flag). Build order outlined: Tier 1 = `--sanctions` + `--climate-future`; Tier 2 = 4 fold-ins (1 PR) + `--match`; Tier 3 = optional transport-noise + 4 medium-novelty. **None transformative** — gap-#6's "buildable vein exhausted" verdict stands for top-level sections; these are surgical depth additions + novel cross-cuts. Depth-survey recorded 8 anti-hallucination catches (HOA-board politics / mosquito / solo-woman / microclimate / wire-vs-BEC-double-count etc. all REJECTED — slot-filling resisted). Defer gap-#7 to ≥2026-11-27. Not greenlit — queued for project owner approval. No version bump.

- **ROADMAP — Batch D queued (13 country candidates, 113 → 126)**. Cross-referenced ISO 3166-1 against the 113-country matrix + Skip list. **8 strong-signal Caribbean** (4 UK Overseas Territories: `ky` Cayman / `bm` Bermuda / `tc` Turks and Caicos / `vg` BVI — all 4 pass the CONTRIBUTING.md sub-sovereign-standalone test from the Crown Deps Batch A decision; plus 4 CBI countries: `kn` St Kitts & Nevis / `ag` Antigua & Barbuda / `lc` Saint Lucia / `gd` Grenada — independent sovereign states with active Citizenship-by-Investment programs and property qualifying routes at USD 270-325k bands). **5 medium-signal additions** (`na` Namibia / `bw` Botswana / `vu` Vanuatu / `tt` Trinidad and Tobago / `pk` Pakistan). All 13 are not on the Skip list. Build order outlined: Phase A schema-decision PR (mirror Batch C #185) → Phase B 8 strong-signal Caribbean ship (113 → 121) → Phase C 5 medium-signal ship (121 → 126). ROADMAP headline status updated from "backlog exhausted" to "section-side exhausted, country-side has Batch D queued". `dm` Dominica explicitly out-of-scope per [[dr-vs-dominica-conflation]]; tribal Skip-list still binding. Not greenlit — queued for project owner approval. No version bump.

### Changed

- docs: prose count cascade 103/109 → 113 (12 sites) ([#200](https://github.com/soreavis/property-deep-dive/pull/200)) — by @soreavis
- docs: refresh install.md release-tag refs 2026.05.1 → 2026.05.59 (6 sites) ([#201](https://github.com/soreavis/property-deep-dive/pull/201)) — by @soreavis
- **ROADMAP refresh — backlog declared exhausted** (post-`/maintain` session 2026-05-27). The doc had multiple stale "queued" / "🟢 TIER-1 candidate" markers describing work that already shipped: the `--renovation` / `--property-management` / `--surveyor` Tier-1 NEW sections (shipped in PRs #154/#153/#155 on 2026-05-15) plus the 5 additional NEW sections from gap-#4/#5 waves (`--inherited-noncompliance` / `--squatter` / `--latent-defect` / `--title-monitoring` / `--seller-withholding` sub-extension); the Batch C Phase B (NL Caribbean `aw`/`cw`/`sx`/`bq` standalone) + Phase C (FR DROM overlay `gp`/`mq`/`gf`/`re`/`yt`) which shipped same-day in PR #186 on 2026-05-27 morning; the SXM developer-fund-opacity gap (now covered by the `sx` playbook); the JP municipality foreign-buyer-justification gating patch (closed UNVERIFIED in #169 per the anti-hallucination contract with universal 国土利用計画法 + 農地法 §3 added instead); the CY expat-targeted scam patterns (now in the CY `--scams` overlay). The doc now opens with an explicit "Status: backlog exhausted" callout; the NEW sections table marks all 7 lines as shipped with PR + date stamps; the Batch C table changes "queued" → "✅ Shipped 2026-05-27 (#186)"; the 3 country-specific patches change from open `🟠` to closed `✅`; a 2026-05-27-evening decision-log entry captures the steady state ("10 Tier-1 NEW sections + 11 sub-section extensions + 1 cross-cutting overlay + Batch A/B/C + TCO additions + url-liveness Python rewrite + 113-country validation sweep all shipped"). Future work returns to normal `--update` cadence via the tier-refresh + regulatory-watch + feed-watcher + health-report workflows. No version bump (drift cleanup).
- docs: ROADMAP refresh — backlog declared exhausted (post-validation-sweep) ([#202](https://github.com/soreavis/property-deep-dive/pull/202)) — by @soreavis
- fix(verify-needed): cleanup 4 inline markers (IT/KZ/AE/FO) — 3 anti-hallucination catches ([#203](https://github.com/soreavis/property-deep-dive/pull/203)) — by @soreavis
- fix(es): 100% non-EU buyer gravamen — verified STALLED + caveat added ([#204](https://github.com/soreavis/property-deep-dive/pull/204)) — by @soreavis
- docs: ROADMAP — queue Batch D (13 country candidates, 113 → 126) ([#206](https://github.com/soreavis/property-deep-dive/pull/206)) — by @soreavis
- docs: ROADMAP — queue 16 feature candidates (sections + extensions + fold-ins + regwatch) ([#207](https://github.com/soreavis/property-deep-dive/pull/207)) — by @soreavis
- docs: ROADMAP dedup country-specific patches (PR #202 follow-up) ([#205](https://github.com/soreavis/property-deep-dive/pull/205)) — by @soreavis
- chore(maintain): doc-hygiene sweep — stale README counts + 2 broken playbook links ([#208](https://github.com/soreavis/property-deep-dive/pull/208)) — by @soreavis
- docs(regwatch): ship 4 always-on regulatory-watch entries (verified, 2 corrections) ([#209](https://github.com/soreavis/property-deep-dive/pull/209)) — by @soreavis
- docs(batch-d): Phase A schema decision — 4 UK OTs verified standalone (research + audit) ([#210](https://github.com/soreavis/property-deep-dive/pull/210)) — by @soreavis
- feat(sanctions): add --sanctions, the 40th section (seller/UBO/PEP screening) ([#211](https://github.com/soreavis/property-deep-dive/pull/211)) — by @soreavis
- feat(batch-d): Phase B — 8 Caribbean playbooks (113 → 121 countries) ([#212](https://github.com/soreavis/property-deep-dive/pull/212)) — by @soreavis
- fix(batch-d): restore SKILL.md country matrix to 121 rows (matrix audit) ([#213](https://github.com/soreavis/property-deep-dive/pull/213)) — by @soreavis
- feat(climate): forward + near-property-resolution projection layer ([#214](https://github.com/soreavis/property-deep-dive/pull/214)) — by @soreavis
- feat(backfill): Batch D cross-cutting backfill Wave 1 (registry/fraud cluster) ([#215](https://github.com/soreavis/property-deep-dive/pull/215)) — by @soreavis
- feat(backfill): Batch D cross-cutting backfill Wave 2 (finance/tax/transaction) ([#216](https://github.com/soreavis/property-deep-dive/pull/216)) — by @soreavis
- feat(backfill): Batch D cross-cutting backfill Wave 3 — completes the backfill (28/28 sections) ([#217](https://github.com/soreavis/property-deep-dive/pull/217)) — by @soreavis
- fix(connectivity): correct 2 doubled-path playbook cross-links (lychee CI) ([#219](https://github.com/soreavis/property-deep-dive/pull/219)) — by @soreavis
- fix(playbooks): corpus-wide anti-hallucination consistency sweep (112 playbooks) ([#220](https://github.com/soreavis/property-deep-dive/pull/220)) — by @soreavis

- chore(deps): bump actions/setup-python from 5.6.0 to 6.2.0 ([#225](https://github.com/soreavis/property-deep-dive/pull/225)) — by @dependabot[bot]

- chore(deps): bump softprops/action-gh-release from 2.6.2 to 3.0.0 ([#224](https://github.com/soreavis/property-deep-dive/pull/224)) — by @dependabot[bot]

- chore: slim SKILL.md router + count-drift guards + fix stale 103→121 counts ([#229](https://github.com/soreavis/property-deep-dive/pull/229)) — by @soreavis

- fix(ci): auto-merge-docs reads live labels + re-runs after labeler ([#230](https://github.com/soreavis/property-deep-dive/pull/230)) — by @soreavis

- feat(verify): source-content verifier — harness, deterministic audit, 16 citation fixes ([#231](https://github.com/soreavis/property-deep-dive/pull/231)) — by @soreavis

- fix(verify): silence pypdf chatter + magic-byte guard for .pdf-URLs-serving-HTML ([#233](https://github.com/soreavis/property-deep-dive/pull/233)) — by @soreavis

- fix(verify): harden audit signals — primary-source linter scope + content-change settle-confirmation ([#234](https://github.com/soreavis/property-deep-dive/pull/234)) — by @soreavis

- feat(match): --match reverse-compare discovery mode (Phases 1-2b, audited) ([#235](https://github.com/soreavis/property-deep-dive/pull/235)) — by @soreavis

- chore(maintain): fix stale section + tier counts ([#236](https://github.com/soreavis/property-deep-dive/pull/236)) — by @soreavis

- regwatch: re-verify + re-stamp 2 overdue GE entries (closes #196) ([#237](https://github.com/soreavis/property-deep-dive/pull/237)) — by @soreavis

- fix(source-verify): don't false-flag JS-SPA / thin-body pages as TOKENS_ABSENT ([#238](https://github.com/soreavis/property-deep-dive/pull/238)) — by @soreavis

- feat(foldins): 4 Tier-2 fold-ins (LGBTQ+ title, accessibility, EV-charger, wire-settlement) ([#239](https://github.com/soreavis/property-deep-dive/pull/239)) — by @soreavis

- feat(auction-registry): 41st section — distressed-auction discovery + system classification ([#240](https://github.com/soreavis/property-deep-dive/pull/240)) — by @soreavis

- fix(source-verify): #232 triage — 5 figure corrections + 27 repoints + 16 dead-link repairs ([#241](https://github.com/soreavis/property-deep-dive/pull/241)) — by @soreavis

### Fixed

- **Dead-link triage final pass — 7 of the 9 deferred gov/HUD links repointed to verified successors; 2 kept DEPRECATED with the identified successor named (no version bump).** Resolved the maintenance items deferred from #244/#245 (successors that were geo/TLS-blocked from the build/audit environment). A focused 5-agent verification (agent egress reaches `.gov` hosts this session's curl/WebFetch cannot) fetch-verified or live-corroborated 7 successors → repointed, removing their DEPRECATED markers: **HUD** `program_offices/housing/sfh/ins/203b--df` → `hud-partners/single-family-sfh203b` (exact live title indexed; HUD migrated sfh/ins → /hud-partners/); **NCA5** `nca2023.globalchange.gov` → `repository.library.noaa.gov/view/noaa/61592` (NOAA Institutional Repository, DOI 10.7930/NCA5.2023 — the canonical official rehost after globalchange.gov was taken down 1 Jul 2025; fetch 403 = alive); **PL** `punktyadresowe.uke.gov.pl` → `mapbook.uke.gov.pl` (UKE Broadband Atlas — confirmed by the HTTP-200 UKE newsroom page naming it); **QA** `qari.gov.qa` → `geoportal.gisqatar.org.qa` (Qatar Geoportal, CGIS/Ministry of Municipality — fetch HTTP 200); **TN** `ancsep.gov.tn` → `ancsep.rns.tn` (ANCSEP on Tunisia's health-network domain); **CV** `protecaocivil.cv` → `snpcb.cv` (SNPCB); **BZ** `lands.gov.bz` → `naturalresources.gov.bz` (Lands & Surveys now under the Ministry of Natural Resources). The remaining **2 (MX `rppyc.cdmx.gob.mx`, ID `sentuhtanahku.atrbpn.go.id`)** stay DEPRECATED — strong search evidence of a live same-authority successor (`data.consejeria.cdmx.gob.mx/index.php/rppyc`; `atrbpn.go.id/sentuh-tanahku`) but neither the agents nor this session could fetch-verify them (`.gob.mx`/`.go.id` datacenter-blocked), so per the never-repoint-to-an-unverified-URL rule their markers were upgraded to NAME that successor + "fetch-verify from an unblocked network" rather than applied. markdownlint 0, forbidden-phrasings clean.

- **All-MD maintenance sweep — stale current-state count claims + imprecise cross-references corrected (no version bump).** Full `/maintain` pass over all 219 markdown files (markdownlint 0 errors). Fixed drift that the `sync-docs` bold-`**N**`-only regex doesn't catch: (1) the "121 countries × **40 sections**" rationale lines in `shared/regulatory-watch.md` + `shared/updater.md` → **41 sections**; (2) eight bare-prose cross-references pointing to filenames that don't exist (not clickable links, so the CI link-check correctly ignored them, but misleading): `shared/mains.md`→`shared/mains-reliability.md` (connectivity.md, esg.md), `shared/climate.md`→`shared/climate-projections.md` (esg.md), `shared/visa.md`→`shared/visa-programs.md` (digital-nomad-healthcare.md), `shared/data-honesty.md`→`shared/integrity-checks.md` (sx/playbook.md `--integrity`), `shared/tax.md`→"the property-country `--tax` layer (per-country playbook)" (home-tax.md, no shared/tax.md exists), `shared/risks.md`→"per-country playbook `--risks` section" (risks-build-quality.md ×3, no shared/risks.md exists); and (3) a misleading `countries/dm/playbook.md` (Dominica) reference in aw/playbook.md → reworded to note Dominica's CBI is out of scope (no `dm` playbook). The `tz`/`ug` forward-references in ke/playbook.md were left as-is — they are honestly hedged with "when added". markdownlint 0, forbidden-phrasings clean, sync-docs in-sync.

- **Dead-link triage follow-up — the 28 surfaced genuinely-dead URLs DEPRECATE-marked (no version bump).** Completed the second half of the dead-link triage: each of the 28 dead URLs with no verified-live successor is now annotated inline `❌ DEPRECATED — link dead (verified 2026-06-13); verify with <fallback authority>` (the established corpus marker), at **every** occurrence (45 marks across ~20 files). Split: ~16 **defunct private listing/forum sites** (Israemploy ceased 2021, Khmer440 closed Sep-2024 [already marked], the non-existent `property24.{co.rw,com.gh}` country instances [Property24 is South-Africa-only], `propertyguru.co.id`/Rumah.com ceased Dec-2023, `properati.com.py` [Properati is not in PY], `propertyfindermontenegro`, `propertyhub88`, `alaventa.pe`, `jordanrealestate.com.jo`, `developers.funda.nl` [no public dev portal], etc.) → marked with a "use a current portal / verify with the operator" fallback; ~9 **gov registries whose successor could not be fetch-verified from this environment** (geo/TLS-blocked: PL `punktyadresowe.uke.gov.pl`→UKE/mapbook, MX `rppyc.cdmx.gob.mx`→Consejería Jurídica, ID `sentuhtanahku.atrbpn.go.id`→ATR/BPN, BZ `lands.gov.bz`→Min. Natural Resources, TN `ancsep.gov.tn`→Min. Health, QA `qari.gov.qa`→Min. Municipality GIS, US `nca2023.globalchange.gov`→globalchange.gov/NOAA [NCA host taken down 30-Jun-2025], CV `protecaocivil.cv`→SNPCB) → marked with the named fallback authority (NOT a fabricated live successor URL); EG `sherpa.gov.eg` + SA `sahel.mt.gov.sa` dead subdomains marked, pointing to their already-co-cited live parents (`moj.gov.eg` / `mt.gov.sa`). **Self-correction**: `reir.rw` (a defunct *private* Real-Estate-in-Rwanda association) was NOT repointed to the gov land authority `lands.rw` — different entity (same trap as the earlier RW fix); marked DEPRECATED with both RDB/RLMUA named as where to verify. No live successor was fabricated; the handful blocked only by this environment's reachability are flagged for a truly-clean-network re-check. markdownlint 0, forbidden-phrasings clean.

- **Dead-link triage — 43 genuinely-dead URLs repointed to verified-live successors; ~89% of the "confirmed-dead" backlog confirmed false-positive (no version bump).** The url-liveness cache held **675 fail_streak≥3 "confirmed-dead" URLs** (the run still scored 91% ≥ the 90% gate, so no CI issue fired). Per the project's re-verify-before-mass-action rule, all 663 still-present-in-corpus URLs were **browser-UA re-validated** (37-agent fan-out) before any edit: **591 (~89%) were false-positives** — `UNCERTAIN` 449 + `BLOCKED` 50 + `LIVE` 92 (slow / geo / bot-blocking government hosts that time out the CI's async aiohttp client but load fine in a browser) — confirming a mass-edit would have corrupted ~591 good citations. Only **73 were genuinely dead**: **43 repointed** to a fetch-verified (HTTP 200) same-authority successor (e.g. BoI `/he/Markets/…` → `/en/economic-roles/financial-markets/exchange-rates/`; HK `landregistry.gov.hk` → `landreg.gov.hk`; SA `premiumresidency.sa` → `pr.gov.sa`; MD `geoportal.md` → `asp.gov.md`; KZ `pravstat.prokuror.gov.kz` → `qamqor.gov.kz`; AL `albtelecom.al` → `one.al`; LI Volksblatt → `vaterland.li`; typo/TLD fixes corAsaan→corAAsan, `.com.py`→`.org.py`), across 32 playbook/shared files. **Self-correction caught in verification**: the agent's `rlma.rw → rdb.rw` repoint was factually wrong (RLMA became **RLMUA** at `lands.rw`, not the separate Rwanda Development Board) and was hand-fixed to `lands.rw` in 3 places. The remaining **28 genuinely-dead-no-clean-replacement** URLs (defunct private listing/forum sites + a few gov registries whose successor could not be fetch-verified this run) are **surfaced for a clean-network DEPRECATE pass**, not mass-marked (several likely have live successors blocked only by the run's WebFetch flakiness). Also added `example.gov` (IANA-reserved doc placeholder) + `overpass-api.de` (live OSM POST API, 404s on GET) to `config/_url-liveness.json` `excluded_hosts` to stop recurring false-positive noise. 1 search-only-verified repoint (HUD 203(b)) deliberately **held** pending fetch-verification rather than applied unverified.

- **`auto-merge-docs.yml` never enabled auto-merge on PRs opened without the `documentation` label.** Root cause: the path-based labeler applies `documentation` with the default `GITHUB_TOKEN`, and GitHub's recursion-prevention means a label set by `GITHUB_TOKEN` does **not** emit a `labeled` event that re-triggers downstream workflows — so the enabler only ever saw the `opened`/`synchronize` payloads (when the label isn't applied yet) and skipped forever (surfaced by PR #229, which carried `documentation` yet skipped and had to be merged manually). Fixed two ways: (1) the workflow now **reads the PR's current `state`/`isDraft`/`author`/`labels` from the API inside the job** instead of trusting the stale event payload, and (2) it re-evaluates **after the labeler finishes** via a `workflow_run` trigger keyed to `Path-based labeler`. The "documentation-only" safety gate and the minimal-privilege `GITHUB_TOKEN` on the labeler are unchanged — this only closes the label-applied-after-open timing race.

- **Stale `103`-era country counts across user-facing docs (surfaced by the new prose guard).** **11 country-count prose sites** corrected `103 → 121`: README (`--update (all 103)`), the broken `#country-support--103-fully-populated` anchor target in `docs/usage.md`, `docs/README.md`, and **7 SKILL.md** sites (maintenance-cadence table, reform-window line, schedule example, file-tree comments, crime-sources coverage). README's **Contributing paragraph (L403-407)** was internally contradictory (heading said "121 fully populated", body said "All 103 in-scope countries are populated") and listed Crown Dependencies + Danish territories + NL/Caribbean as *still blocked* — those waves are exactly what took coverage 103 → 109 → 113 → 121; rewritten to the real progression with the current backlog (Phase C: na/bw/vu/tt/pk → 126, not greenlit). **Shared-section denominators** reconciled via the scope-vs-count rule: 5 obsolete `"6 region blocks cover all 100 countries … 3 schema-blocked territories default to parent"` meta-sentences (the documented pre-Crown-Deps follow-up — premise now false) rewritten to scope language; `113 → 121` scope claims in `sections.md` (×6) / `language.md` / `remote.md` (×2); coverage-footer denominators `100 → 121` in `schools` / `risks-build-quality` / `digital-nomad-healthcare` / `notary-forced-heirship` / `finance-banking` (the `~70`/`~75` numerators left as author judgment, never blanket-bumped). GitHub About blurb corrected off-tree `113 × 39 → 121 × 40`. Workflow-count cascade `28 → 29` (37 YAML) via `sync-docs.py`. Verification: sync-docs `--check` in sync, prose guard clean, markdownlint 0/215, version aligned `2026.05.65`.

- **Corpus-wide anti-hallucination consistency sweep — all 121 country playbooks**. A 12-batch (10-at-a-time) multi-agent sweep ran every playbook through a per-country pipeline — **audit** (flag contract breaches) → **adversarial-verify** (independent skeptic kills false positives) → **fix** (precision editor, never fabricate a source) → **re-audit/validate** (confirm resolved + hunt *injected* fabrications); ~420 agents across 14 workflow runs. **~711 findings flagged → 457 confirmed (~36 % killed as false positives by the verify stage) → 112 playbooks edited** (9 were already clean: `ag`/`aw`/`cw`/`im`/`kn`/`ky`/`lb`/`lc`/`qa`); net +695 / −620 lines, playbooks only. **Dominant breach class: internal contradictions from incomplete prior corrections** — a corrected, sourced `--tax`/`--visa` section coexisting with stale copies in `--rental`/`--retirement`/`--compare`/cost-tables/Status-footers; every fix **reconciled to the playbook's own already-sourced value**, never to a new fact. Notable catches: **AD** ITP split (`1.5%+2.5%` → statute-cited `1% Govern + 0.50–3% Comú`) + passive-residency floor €600k → €800k (Llei 5/2025) across 6 sites; **AE** Status-footer citing a "Federal Decree-Law 21/2021" the body says does not exist → `Abu Dhabi Law 13/2019` + stale DET permit `AED 3,720` → `1,520/370`; **AU** QLD "Form 1" → "Form 2 Seller Disclosure Statement" (Property Law Act 2023) + NSW FHBAS "indexed annually" walked back to match the body; **AZ/AM** CGT-exemption claims the `--tax` section itself brands uncorroborated; **OM** a self-contradictory currency peg (`3.4490 OMR per USD` — impossible given the parenthetical `0.3850`) reconciled to the file's consistent `0.3850`; **MO** superseded 10 %/80 %-rebate figures + a pre-existing `80k → 90k` stamp-duty arithmetic error; **RW** South-Africa template-leak residue (`ZAR`/`Stats SA`) removed. **Fabrication guard (dual net — validate stage + a whole-tree new-URL set-difference scan): 5 source-provenance issues caught and corrected** (CA constructed URL slug → real verification path; GR + MC editor-introduced links/domains → plain-text authority references; LU unanchored `IGSS` source → anchored `STATEC`; NO injected `skatteloven § 9-3` — *correct in the real world but ungrounded in-file*, de-specified) → **0 surviving fabrications** (final new-URL scan empty; corpus-wide template-leak scan clean). These are **grounding/consistency repairs against each playbook's own sourced content**, so per the CLAUDE.md flow **no new `regulatory-watch.md` entries were added and no `Last verified` dates were re-stamped** (primary sources were not re-verified, only reconciled). Pre-commit gates: **markdownlint 0 errors** (16 MD049 underscore-emphasis nits from the editor agents corrected to asterisk), **0 forbidden phrasings introduced**, **0 new external URLs**. Version bump deferred to release-cut (factual-correction batch — qualifies for a CalVer micro per the scheme above).
- **Internal-link fix in `shared/connectivity.md`** — the Wave-3 backfill added two cross-links to the KY + TC playbooks with a doubled path (`../../skills/property-deep-dive/countries/...`, which resolves to `skills/skills/...`), tripping the Lychee internal-link CI check on `main`. Corrected to the canonical `../countries/<iso>/playbook.md` form (matching `fr-drom-overlay.md`'s playbook cross-links). No version bump.
- **Maintenance sweep (`/maintain` 2026-05-28)** — doc-hygiene corrections surfaced by a full chore pass. (a) README "Skill content" stat line was stale and internally inconsistent — claimed `166 markdown files, ~83,500 lines (… 48 top-level shared/ …)` when the breakdown's own addends summed to 176 and the skill payload actually holds **193 markdown files / ~94,900 lines** (1 SKILL.md + 65 top-level shared/ + 14 shared/exit/ region files + 113 country playbooks); corrected. `sync-docs.py` also refreshed the "Repo total" line (`~97,100 → ~97,200` lines). (b) Two broken relative links to `shared/regulatory-watch.md` fixed: `countries/gg/playbook.md` had a doubled path (`../../skills/property-deep-dive/shared/…`) and `countries/je/playbook.md` had `../regulatory-watch.md` (wrong depth + missing `shared/`); both now use the canonical `../../shared/regulatory-watch.md` matching the 7 other playbook links. Sweep verification: secret/PII scan clean, markdownlint 0 errors across 206 files, 36 url-liveness unit tests pass, version triple-check aligned at 2026.05.59, 0 orphan files. No version bump (doc hygiene).

- **ROADMAP — dedup country-specific patches** (post-PR #202 follow-up). PR #202 added ✅ closure lines for the 3 country-specific patches (JP / CY / SXM) under `### Country-specific patches (Reddit gap-analysis 2026-05-10)` but didn't delete the original 🟠 lines that came before them, so the section listed each of JP + CY twice (one 🟠 open + one ✅ closed). Consolidated to a single ✅ entry per country with the original 🟠 context preserved inline as "(was 🟠 — ...)" so the closure rationale stays auditable. ROADMAP now has 0 open-marker hits per `grep -cE '🟢 \*\*TIER|🟠 \*\*|queued — Phase'`. No version bump (documentation cleanup).

- **ES 100 % non-EU buyer gravamen — verified STALLED + caveat added** (`countries/es/playbook.md` § Tax + `shared/regulatory-watch.md` line 678). Surfaced by Reddit gap-analysis #6 as a possible coverage gap (the ES playbook didn't mention this widely-discussed proposal anywhere — only the regwatch watchlist did, and as a 1-line "floated 2025-01"). Verify-first agent confirmed: **NOT in force**. PSOE [Proposición de Ley 122/000196](https://www.congreso.es/es/proyectos-de-ley?p_p_id=iniciativas&p_p_lifecycle=0&p_p_state=normal&p_p_mode=view&_iniciativas_mode=mostrarDetalle&_iniciativas_legislatura=XV&_iniciativas_id=122/000196) (BOCG B-229-1, 30/05/2025) was registered 22 May 2025, qualified 27 May, advanced to "Pleno toma en consideración" stage 5 September 2025, and has **never reached a floor vote** in 14+ months. Government quietly dropped the measure from its January 2026 housing package; Reuters confirmed no parliamentary debate on 27 March 2026; Junts withdrew government support (supply-side argument); Podemos opposes from the left (insufficient — wants outright ban); no BOE-published Ley exists. Added a ⚠️ caveat block at the top of ES § Tax explicitly stating "NOT in force" with primary cites, so the single most-Googled Spanish property regulation by non-EU buyers since January 2025 gets an authoritative answer instead of a silent omission. Upgraded regwatch entry from 1-line "floated" to STALLED with bill number, BOCG cite, Reuters confirm, and a clear "Do NOT factor into TCO models" instruction. **Anti-hallucination catches by the agent**: (a) bill is a **Proposición de Ley** (parliamentary-group), NOT a Proyecto de Ley (government bill) — different procedural milestones; (b) easy-confusion-trap with `121/000023` which is the unrelated multinationals' global minimum tax (both use "Impuesto Complementario" in the name); (c) **BOE-A-2025-6598 / Real Decreto 252/2025** is the regulation of that multinationals' tax, NOT the non-EU buyer gravamen — flagged inline so future maintainers don't mistake it for evidence of enactment. Bill text scope (NOT law): 100 % of catastral reference value, second-hand residential only (new-builds excluded under IVA), non-EU non-residents, foral PV+Navarra exempt; PwC + Bravos legal analyses flag TFEU Art. 63 (free movement of capital) risk if ever enacted. No version bump (drift cleanup / regwatch refinement).

- **Reddit gap-analysis #6 — buildable backlog CONFIRMED EXHAUSTED**. Research agent surveyed ~140 threads across 50+ subreddits (19 distinct queries, 12 of ≤16 web-call budget used) for the 12-day window 2026-04-01 → 2026-05-27. **0 new buildable section/extension candidates surfaced.** The 6 items the agent flagged (AU foreign-buyer ban 1 Apr 2025, JP FY2026 nationality-declaration, UK Renters' Rights Act 1 May 2026, PT golden-visa citizenship law, CA foreign-buyer-ban sunset chatter, ES 100 % gravamen) are all already-covered or normal `shared/regulatory-watch.md` material — exactly the maintenance channel that already exists. The 4 gap-#5 NOT-BUILDABLE rejections (expropriation, environmental-contamination liability, insurance-claim-as-non-resident, foreign-heir partition-deadlock) still hold — no new primary-source evidence in the 12-day window. Meta-validation catch: the agent's brief didn't include the country matrix as an inventory anchor, so it flagged "Saudi Arabia opening Jan 2026" as a country-add candidate — but `countries/sa/playbook.md` already exists (1,200 lines, validated batch 6 of the 2026-05-27 sweep) — false positive. Recommendation: do NOT run gap-#7 for ≥6 months — the 12-day cadence is too short for true cross-country signal accumulation, and gap-#5's "buildable vein is exhausted" conclusion still holds. Full memo at `_local/reddit-gap-analysis-6.md` (gitignored). Only actionable item from the entire run: the ES 100 % gravamen status update (above).

- **VERIFY-NEEDED inline-marker cleanup — 4 markers verified against primary sources (4 country playbooks · 4 parallel research agents)**. Task asked: clear the carry-over verify markers left by the 2026-05-27 113-country validation sweep. Approach: 4 parallel research agents (≤16 web calls each, write-to-file early, strict NEVER-FABRICATE clause) → 4 memos in `_local/wip-verify-needed/` → integration pass.

  **IT — Cedolare secca line 145** ([`countries/it/playbook.md`](skills/property-deep-dive/countries/it/playbook.md)): two meta-validation catches by the research agent against my own brief: (1) the "30 % flat rate for 3°/4° immobile NEW bracket from 2026" was a parliamentary draft proposal that did **NOT** make it into the enacted L. 199/2025 text (Agenzia Entrate's April 2026 official guide + normattiva.it consolidated text confirm only the 21 %/26 % two-rate scheme survives); the claim had propagated through many Italian commercialista websites because the proposed-but-not-enacted text was widely echoed by secondary aggregators; (2) the playbook's "5°+ immobile → P.IVA" threshold was wrong — Agenzia Entrate confirms the P.IVA presumption kicks in at **3°+** (≤2-cap and ≥3-impresa-presumption are the same threshold expressed two ways, not distinct thresholds). Replaced with verified text citing L. 199/2025 art. 1 c. 17 modifying L. 178/2020 art. 1 c. 595 + DL 50/2017 art. 4 commi 2-3 + L. 213/2023 art. 1 c. 63 (rate scheme).

  **IT — Sismabonus line 352**: VERIFIED — L. 199/2025 art. 1 c. 22 modifying DL 63/2013 art. 16 confirms 50 %/36 % (2025+2026), 36 %/30 % programmed for 2027, €96,000/unit cap, 10 quote annuali. VERIFY-NEEDED marker replaced with the exact comma reference + underlying-statute pointer (art. 16-bis TUIR + art. 16 c. 1-septies DL 63/2013).

  **KZ — Social tax line 153** ([`countries/kz/playbook.md`](skills/property-deep-dive/countries/kz/playbook.md)): VERIFIED — rate cut 11 % → 6 % from 1 Jan 2026 under new Tax Code K2500000214 (Law 214-VIII ZRK signed 18 Jul 2025). Sharpened the "no reduction for social contributions" qualifier: (a) the old offset (СН minus social contributions) is **eliminated** so the headline 6 % is the effective rate; (b) employer compulsory pension contribution **ОПВР rises from 2.5 % → 3.5 % in 2026** on a phased 2024-2028 trajectory (1.5 % → 2.5 % → 3.5 % → 4.5 % → 5 %) — net employer payroll burden ≈ unchanged. Should NOT be framed as a tax cut for landlords/buyers without that context. Sectoral rates added (agricultural 1.8 %; ИП 2 MCI/month + 1 MCI/employee; крестьянские хозяйства 0.6/0.3 MCI; special-tax-regime users exempt). Primary cite: State Revenue Committee Kostanay (kst.kgd.gov.kz) + ENPF. PENDING-PRIMARY-VERIFICATION marker removed. Added a `shared/regulatory-watch.md` KZ entry covering this cut with the offset/ОПВР context. adilet.zan.kz TLS issue prevented article-level pinpointing inside K2500000214 — flagged as a future maintenance follow-up; the SRC primary-government source is sufficient for the rate itself.

  **AE — Abu Dhabi benchmarks line 140** ([`countries/ae/playbook.md`](skills/property-deep-dive/countries/ae/playbook.md)): "data not retrieved during this run" gap filled with a 12-row benchmark table covering Saadiyat / Yas / Al Reem (luxury slice + whole-island + live-index splits where they diverge meaningfully) + Abu Dhabi citywide apt/villa/blended — sourced to **Knight Frank H1 2025** (transaction-weighted) + **Bayut FY 2025 + H1 2025 + Index April 2026** (listing-aggregate) + **ValuStrat Q3 2025** (hedonic index) + **ADREC 2025 annual report** (direction confirm: +19 % apartments, +13 % villas YoY 2024 → 2025). All zones rated **VERIFIED-SECONDARY** — no zone is VERIFIED-PRIMARY because ADREC's per-zone AED/sqft is interactive-dashboard-only (no static export); manual dashboard visit at `adrec.gov.ae/en/property_and_index/adrec-dashboard` required for definitive transactional values. Anti-hallucination catch: the prior anecdote "Saadiyat villas often AED 2,500–4,500/sqft" was **HIGH** — that band describes the top slice (Mariah Island branded beachfront), but the H1 2025 luxury-segment median is AED 2,043/sqft and the whole-island median (incl. Reserve/Lagoons) is AED 1,631. Now reflected explicitly. Rental yields (Bayut FY 2025 ROI) added: Saadiyat villa 4.47 % · Saadiyat apt 4.05 % · Yas villa 5.43 % · Yas apt 7.07 % · Al Reem apt **7.49 %** (highest mid-tier yield in AD). Methodology note explains the Knight Frank vs ValuStrat AED 1,005 vs AED 1,296 citywide spread (transaction-weighted vs hedonic — both correct).

  **FO — Foreign-buyer law line 587** ([`countries/fo/playbook.md`](skills/property-deep-dive/countries/fo/playbook.md)): VERIFIED — [Løgtingslóg nr. 158 frá 10. desember 2021 um at keypa fastogn í Føroyum](https://logir.fo/Logtingslog/158-fra-10-12-2021-um-at-keypa-fastogn-i-Foroyum). Løgtingið passed it on **7 December 2021** (kvf.fo + dagur.fo cross-confirm); statute dated **10 December 2021**; published (kunngjørd) **13 December 2021**; entered into force **14 December 2021** per § 13 stk. 1 ("Henda løgtingslóg kemur í gildi dagin eftir, at hon er kunngjørd"); § 13 stk. 2 grandfathers pre-14-Dec-2021 transactions in flight. Anti-hallucination note: the playbook's earlier "13 December 2021" claim was NOT fabricated — it was the publication date (kunngjørd / Útgávudagur), not the statute date; the conservative softening to "December 2021" was correct given the ambiguity in local.fo, but the chronology can now be cited precisely. Faroese statute name preserved verbatim per the foreign-language preservation policy. No pre-2021 operative statute was located — this is original 2021 legislation, not an amendment. The verify-at-logir.fo flag is removed; logir.fo is now cited inline. Regwatch promotion entry line 578 also updated with the same statute reference. Closes [[stale-count-files]]-tracked verify-needed for FO.

  **Meta**: 3 of 4 markers had real anti-hallucination catches (IT 30%-bracket fabrication, IT 5°→3° threshold error, FO publication-vs-statute-date conflation, AE anecdote-high-for-median). Reinforces [[meta-validation-catches-maintainer-briefs]] — strict NEVER-FABRICATE briefs catch maintainer drift at ≈75 % rate (3/4) when applied to verify-needed cleanup. No version bump (drift cleanup).

- **`docs/install.md` release-tag refresh** — 6 hardcoded references to release tag `2026.05.1` (58 versions stale) updated to current latest `2026.05.59`. Lines affected: `git checkout` example (L100) + 5 instances in the `gh release download` + `cosign verify-blob` sigstore-verify command block (L110-118). The previous `2026.05.1` literal would have caused copy-paste failure for users following the verify-authenticity instructions because the corresponding tarball + sigstore bundle were never published for that pattern. Also bumped the `# or any later tag` placeholder comment's anchor to match. Caught by post-`/maintain` audit when the user asked "are all doc files up to date" — `/maintain` Phase 3c had scanned root MDs but did NOT extend to `docs/*.md` tag-literal scanning; both `/refresh` and `/maintain` skill specs amended (`~/.claude/skills/refresh/SKILL.md` Action 3 + new Action 11 / `~/.claude/skills/maintain/SKILL.md` Phase 3d extension + new Phase 3g) so future runs catch (a) literal release tags in copy-paste shell commands across docs/ and (b) the silent GitHub repo About blurb. Also fixed the GitHub repo About description (off-tree, no commit needed) — was "across 103 countries × 39 sections" → now "across 113 countries × 39 sections". No version bump.

- **Count-cascade drift cleanup (post-113-country sweep)** — `/maintain` Phase 3c pass found 12 prose-form mentions of stale country counts (`109` or `103`) that `sync-docs.py`'s `**N countries**` bold-pattern regex doesn't catch. Updated forward-facing prose to `113` in: `README.md` (3 sites — headline + tree-diagram comment + skill-content stat), `CLAUDE.md` (2 sites — file map + anti-hallucination example), `ROADMAP.md` (2 sites — intro framing + Tier-C count `49→59`), `.claude-plugin/plugin.json` (1 — opening description clause), `.github/ISSUE_TEMPLATE/new-country.yml` (1 — duplicate-country checklist label), `shared/regulatory-watch.md` (1 — corpus-size rationale), `shared/updater.md` (1 — refresh-cadence framing), `shared/exit.md` (1 — May 2026 snapshot batch label), `shared/remote.md` (2 — POA universal-fallback + Top-7 mitigation). Historical CHANGELOG / batch-shipment logs / dated validate reports intentionally LEFT UNCHANGED. `100 of 103 countries` extension-coverage denominators left alone (extensions written against the legacy 103 corpus, the 4 new playbooks aren't backfilled into the cross-cutting tables — overclaiming to `113` would be silent fabrication). Closes the documented [[stale-count-files]] cascade for 2026-05-27 evening's `109 → 113` corpus expansion. No version bump (drift cleanup, not a new feature).

- **5 source-grounded factual corrections from the #232 source-content triage (no version bump).** Surgical triage of the 2026-06-02 deterministic audit (141 `TOKENS_ABSENT` + 250 `SOURCE_UNAVAILABLE`): deterministic FP-bucketing (56 high-confidence false-positives — statute-numbers / historical-dates / years-in-law-names cited against homepages) → a 95-claim quote-or-abstain **Workflow** verify (batches of 8) with an **adversarial refute pass** → maintainer re-verification against each primary source. Of the flags, **5 were genuine figure errors** (the rest tokeniser false-positives, homepage-cited-deep-facts, or mis-citations where the figure itself was correct). Each fix re-checked live before edit: **EC** Salario Básico Unificado 2026 **USD 470 → 482** (+USD 12; 470 was the 2025 value) per [trabajo.gob.ec](https://www.trabajo.gob.ec/); **IN** non-fossil installed-capacity share **~46 % → 53.6 %** (~288 GW) per [MNRE](https://mnre.gov.in/); **BS** apostille/legalization cost **~$40-100/page → official MOFA $20/document** (+$20/copy, +$10/subsequent-signature) per [mofa.gov.bs](https://mofa.gov.bs/consular-services/apostilleslegalization-of-documents/) (`shared/language.md`); **US estate-tax treaty list 16 → 15 current partners** (Norway + Sweden dropped from the current IRS table) per [IRS](https://www.irs.gov/businesses/small-businesses-self-employed/estate-gift-tax-treaties-international) (`shared/home-tax.md`); **PA** "2024 M6+ events documented" **→ zero ≥M6 recorded in 2024** ([USGS](https://earthquake.usgs.gov/) catalogue returns count 0 for the cited box; sanity-checked against global M6+ Jan-2024 = 12). Grounding-only — one figure per file, no `Last verified` re-stamp (whole-playbook currency unchanged).

- **#232 citation backlog actioned — 27 repoints + 16 dead-link repairs (no version bump).** Following the figure fixes above: **27 citation repoints** applied (figure already correct but cited to a homepage/shallow page → pointed to the page that actually substantiates it, each carrying a verbatim quote from the verify/refute run) — gated to **same-registrable-domain** swaps so link labels stay honest (e.g. `ciu.gov.kn/` → `ciu.gov.kn/real-estate-investment/`, `cnb.cz` → its LTV-limits page), plus 2 cross-domain rewrites with corrected labels (AG → the actual Non-Citizens Land Holding Act PDF on `laws.gov.ag`; TW → the Climate Change Response Act text on the MOJ law database). **16 of 18 genuinely-dead 404 links** repointed to verified current official pages via an 18-agent dead-link **Workflow** (each replacement liveness-checked + quote-confirmed): the BOFiP DOM-TOM tax doctrine refs re-resolved by their stable BOI identifier (BOFiP restructured the URLs), the UK **Renters' Rights Act 2025** chapter corrected (`ukpga/2025/40` → `2025/26`), `gov.uk` council-tax → the MHCLG empty/second-home premium guidance, `rijksoverheid.nl` NHG → its `volkshuisvestingnederland.nl` successor. **Deferred (documented on #232), by design:** ~17 cross-domain repoints kept on their original primary source where the original was already correct (Thai FET USD-50k trigger is right as cited — the page's 200k is a different instrument; the CO `avisos-de-remate` portal IS the right "where-published" auction source; Federal Register links are canonical), the BS BIA investment page (the only official replacement found belonged to a different department → not auto-applied, to avoid a label-lie), and the MV ministry page (no clean replacement). The 110 `403`/bot-wall "unavailable" URLs (legifrance, ato.gov.au, loc.gov) are alive for humans — left as-is.

### Earlier releases

Releases **2026.04.0 → 2026.05.59** are archived in
[docs/changelog/2026-h1.md](./docs/changelog/2026-h1.md).
